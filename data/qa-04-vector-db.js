/* Vector Databases and Embeddings — 23 questions */
window.QA_SECTION = {
  id: '04',
  slug: 'vector-databases',
  title: 'Vector DBs & Embeddings',
  blurb: 'ANN indexes, similarity metrics, quantization, multi-tenancy, and the migration problems nobody plans for.',
  questions: [
{ q: 'What are embeddings in the context of AI engineering?', tags: ['CRITICAL'], a: `An embedding is a learned dense vector representing an item — text, image, user, product — in a space where geometric proximity encodes semantic similarity. They are the interface layer that turns unstructured data into something math can operate on.<br><br>In practice they power retrieval (RAG), semantic search, deduplication, clustering, recommendation, classification, and anomaly detection — all reduced to nearest-neighbor operations in one vector space.<br><br><strong>The property that drives every engineering decision:</strong> the geometry is entirely a product of the training objective, not a universal fact about meaning. Two models of identical dimensionality produce incomparable spaces; cosine scores are not comparable across models; vectors from different models cannot share an index. That is why changing an embedding model forces a full reindex, why "0.85 similarity" means nothing without a model-specific calibration, and why an embedding trained for symmetric similarity behaves differently from one trained for asymmetric query→passage retrieval.<br><br><strong>Second-order:</strong> embeddings are lossy compressions optimized for their training distribution. Rare tokens, identifiers, and negation are systematically poorly represented — which is exactly why hybrid search with BM25 exists.` },

{ q: 'How do embedding models convert text to vectors?', tags: [], a: `Tokenize the text, run it through a Transformer encoder, then <strong>pool</strong> the resulting token representations into a single fixed-length vector — mean pooling over tokens, the [CLS] token, or last-token pooling for decoder-based embedders. Most models then L2-normalize the output so cosine similarity reduces to a dot product.<br><br><strong>The training objective is what makes the vectors useful.</strong> Contrastive learning with InfoNCE: pull related pairs (a query and its relevant passage) together, push unrelated pairs apart, using large in-batch negatives. <em>Hard negative mining</em> — training against passages that look relevant but are not — is the single biggest driver of retrieval quality, because easy negatives teach the model almost nothing.<br><br><strong>The asymmetry detail that trips people up:</strong> retrieval is not symmetric. A short question and a long passage are different kinds of text, so many models (E5, BGE, GTE) require instruction prefixes like <code>query:</code> and <code>passage:</code>. Omitting them does not error — it silently degrades retrieval, which makes it a classic production bug.<br><br>Note also that pooling discards word order and fine-grained structure; a chunk covering five topics produces an averaged vector that matches none of them strongly, which is the mathematical reason chunk size matters so much.` },

{ q: 'What is the difference between sparse and dense embeddings?', tags: ['FREQUENTLY ASKED'], a: `<strong>Sparse</strong> vectors have dimensionality equal to the vocabulary (tens of thousands to millions) with mostly zeros — each dimension corresponds to an interpretable term. TF-IDF and BM25 are the classic examples; SPLADE is a learned sparse model that expands documents with related terms while keeping the interpretable term-dimension structure.<br><br><strong>Dense</strong> vectors are low-dimensional (384–4096) with all dimensions active, learned so that proximity encodes semantics. No dimension has a human-readable meaning.<br><br><strong>The complementary failure modes are the point:</strong> dense embeddings capture paraphrase and synonymy ("car" ≈ "automobile") but are weak on exact tokens — SKUs, error codes, function names, rare proper nouns — because such strings are underrepresented in training and get smeared into nearby regions. Sparse retrieval nails exact and rare-term matching, with proper IDF weighting, but has no concept of synonymy.<br><br>That is why hybrid search consistently beats either alone, particularly on technical and enterprise corpora dense with identifiers.<br><br><strong>Operational differences worth knowing:</strong> sparse indexes (inverted lists) are cheap, exact, and need no training or GPU; dense indexes need ANN structures, more memory, and a full reindex when the model changes. Sparse retrieval is also directly explainable — you can show which terms matched — which matters in regulated settings.` },

{ q: 'Explain cosine similarity, dot product, and Euclidean distance.', tags: ['FREQUENTLY ASKED'], a: `<strong>Cosine similarity</strong> measures the angle between vectors, ignoring magnitude: <code>(a·b)/(‖a‖‖b‖)</code>, ranging [−1, 1]. It is the standard for text because document length inflates magnitude without changing topic — you want direction, not size.<br><br><strong>Dot product</strong> is <code>a·b</code> — unbounded, and sensitive to magnitude. Some models are deliberately trained so magnitude encodes something useful (importance, popularity, confidence), in which case dot product is the correct metric.<br><br><strong>Euclidean (L2) distance</strong> is straight-line distance, sensitive to both direction and magnitude.<br><br><strong>The relationship that matters in practice:</strong> for L2-normalized vectors all three give the same ranking — cosine equals dot product, and <code>L2² = 2 − 2·cosine</code>, a monotonic transform. Since most modern embedding models output normalized vectors, the metric choice often does not change results, and dot product is then preferred because it is the cheapest to compute.<br><br><strong>The failure to avoid:</strong> using a different metric than the model was trained with. If a model was trained with cosine and you index with unnormalized L2, ranking degrades silently — no error, just worse results. Always check the model card's specified metric, and confirm whether your vector database normalizes on insert or expects you to.` },

{ q: 'What is a vector database, and how does it differ from a traditional database?', tags: ['CRITICAL'], a: `A vector database stores high-dimensional vectors and answers <em>approximate nearest neighbor</em> queries — "find the k most similar vectors to this one" — with sub-linear latency at scale. Examples: Qdrant, Weaviate, Milvus, Pinecone, plus extensions like pgvector.<br><br><strong>The fundamental difference is the query type.</strong> Relational databases answer exact predicates using B-trees and hash indexes, which work because scalar values have a total order you can partition on. High-dimensional vectors have no such order, and exact nearest-neighbor search degenerates to a full scan — the curse of dimensionality means tree-based partitioning stops pruning anything above ~20 dimensions. So vector databases index with graph structures (HNSW) or partitioning plus quantization (IVF-PQ), and deliberately return <em>approximate</em> results.<br><br><strong>That approximation is the defining trade:</strong> you accept ~95–99% recall to get 100x speed. There is no exact-index option at scale, which is unlike anything in a traditional database.<br><br>They also add features relational systems lack: metadata pre-filtering integrated with graph traversal, hybrid sparse+dense scoring, and multi-tenancy at the index level.<br><br><strong>The pragmatic point for interviews:</strong> you often do not need a dedicated one. pgvector handles millions of vectors alongside your relational data with transactional consistency and no second system to operate — the case for a dedicated vector DB starts at scale, or when you need filtered-search recall and hybrid scoring that Postgres does less well.` },

{ q: 'How does Approximate Nearest Neighbor (ANN) search work?', tags: ['CRITICAL'], a: `Exact k-NN requires comparing the query to every vector — O(n) per query, unusable at scale. ANN trades a small amount of recall for orders-of-magnitude speedup. Two dominant families:<br><br><strong>HNSW (graph-based):</strong> builds a multi-layer proximity graph where upper layers are sparse long-range links and lower layers are dense local ones — a navigable small-world structure. Search starts at the top layer, greedily walks toward the query, then descends. Query time is roughly O(log n). Best recall/latency of the common methods, but memory-heavy (the graph itself is significant) and expensive to build.<br><br><strong>IVF (partition-based):</strong> cluster vectors with k-means into <code>nlist</code> cells, and at query time search only the <code>nprobe</code> nearest cells. Cheaper memory and faster to build; recall depends on whether the true neighbor's cell was probed. Usually combined with product quantization (IVF-PQ) for large-scale compressed indexes.<br><br><strong>The dials to know:</strong> HNSW's <code>ef_search</code> (candidate list size at query time) and <code>M</code> (connections per node) trade recall against latency and memory; IVF's <code>nprobe</code> does the same. These are the knobs you actually tune in production.<br><br><strong>The discipline that matters:</strong> measure recall against an exact brute-force baseline on a sample. Recall is invisible in production — a misconfigured index returns plausible-looking results while silently missing the best match, and no latency dashboard reveals it.` },

{ q: 'How do you choose the right embedding model for your use case?', tags: ['TRADEOFF'], a: `Use MTEB to build a shortlist, never to decide — leaderboard position correlates weakly with performance on your corpus, and benchmark overfitting is common. Build a labeled set of real queries with known relevant documents from your own data and measure recall@k and NDCG@10.<br><br><strong>The decisive axes:</strong><br>• <em>Task type</em> — retrieval (asymmetric query→passage) versus similarity (symmetric) versus classification. Models are trained for one; using a symmetric model for retrieval underperforms.<br>• <em>Domain fit</em> — general models degrade on legal, medical, or internal jargon. A smaller domain-tuned model often beats a larger general one.<br>• <em>Dimensionality</em> — drives index memory and search latency directly. Matryoshka-trained models let you truncate with graceful degradation.<br>• <em>Max sequence length</em> — must exceed your chunk size or you silently truncate.<br>• <em>Multilingual</em> — mandatory if queries and documents differ in language.<br>• <em>Hosting</em> — API models make every reindex a bill and an availability dependency; open models (BGE, E5, GTE, Nomic) can be self-hosted and fine-tuned.<br><br><strong>Weigh the switching cost up front:</strong> changing models means re-embedding everything, so this is expensive to reverse. And note the ordering that surprises people — adding a cross-encoder reranker usually buys more quality than upgrading the embedder, at lower cost and with no reindex. Try that first.` },

{ q: 'What is embedding dimensionality, and how does it affect performance and cost?', tags: ['TRADEOFF'], a: `Dimensionality is the vector length — commonly 384, 768, 1024, 1536, or 3072. It sets a capacity ceiling on how much information the vector can encode, and it drives cost linearly across the whole system.<br><br><strong>Concrete cost:</strong> raw storage is <code>n × dims × 4 bytes</code> in float32, so 10M vectors at 1536 dims ≈ 61GB before HNSW graph overhead (often another 30–50%). Distance computation is O(dims), so search latency scales with it too. Halving dimensions roughly halves memory and compute.<br><br><strong>The quality relationship has strong diminishing returns.</strong> Going 384 → 768 usually buys meaningful accuracy; 1536 → 3072 often buys very little for a doubling of cost. Bigger is not reliably better — a well-trained 768-dim model frequently beats a mediocre 1536-dim one, so dimensionality is not a quality proxy across models.<br><br><strong>Matryoshka Representation Learning is the useful development:</strong> models trained so that prefixes of the vector are themselves valid embeddings. You can truncate a 3072-dim vector to 512 and retain most of the quality, which lets you tune the cost/quality point <em>after</em> training — and enables a two-stage search (retrieve on truncated vectors, rerank on full ones).<br><br><strong>How I'd decide:</strong> measure recall@k at several dimensionalities on your own eval set and pick the knee of the curve. Combined with int8 quantization, most systems land far below their default configuration with no measurable quality loss.` },

{ q: 'How do you handle embedding drift when the model is updated?', tags: ['CRITICAL'], a: `Be precise about the term, because two different things get called drift. <strong>Model-change drift</strong> is a discontinuity: a new embedding model produces vectors in an entirely different space, so old and new vectors are mathematically incomparable. <strong>Data drift</strong> is gradual: the query and document distribution moves away from what the model was trained on, degrading quality with no code change.<br><br><strong>For a model change, the only correct answer is a full reindex</strong> — there is no way to translate old vectors into the new space (learned projection adapters exist in research but are not production-reliable). The safe procedure is blue-green: build the new index alongside the old, evaluate both on a golden query set, then switch atomically behind a flag with a rollback path. <strong>Never re-embed in place</strong>: a half-migrated index mixes incompatible spaces and degrades silently rather than erroring — the worst possible failure mode.<br><br><strong>For gradual data drift:</strong> monitor retrieval quality continuously — click-through or thumbs-up on retrieved results, and periodic evaluation on a fixed golden set. Track the distribution of top-1 similarity scores over time; a downward trend signals queries drifting away from the corpus. Watch for a rising rate of "no good result" queries.<br><br><strong>The operational requirement:</strong> pin the embedding model version in config and store it as index metadata, so you can always tell which model produced which vectors. Teams that skip this cannot safely reason about their own index.` },

{ q: 'What are multi-modal embeddings, and how are they generated?', tags: [], a: `Multi-modal embeddings place different modalities — text, images, audio — in one shared vector space so a text query can retrieve an image directly, with no intermediate captioning step.<br><br><strong>How they are trained:</strong> CLIP is the canonical approach — two encoders (a vision transformer and a text transformer) trained contrastively on hundreds of millions of image-caption pairs, maximizing similarity for matched pairs and minimizing it for mismatched ones within a batch. The two encoders are pushed into alignment; nothing forces a shared space except the objective.<br><br><strong>The critical practical limitation:</strong> CLIP-family models are trained on natural-image captions and are weak on <em>text-heavy</em> images — charts, tables, screenshots, scanned documents — which is exactly what enterprise corpora contain. They also encode coarse semantics ("a dog on a beach") rather than fine detail, so they fail on queries requiring reading the image.<br><br><strong>The pragmatic alternative</strong> for document-style content: run a vision-language model over each asset to generate a rich text description, embed that, and keep the image reference. Retrieval stays single-modality (so hybrid search still works, and you need no new index type), and quality on charts and tables is far better. The cost is a VLM pass per asset at ingest — cache by image hash.<br><br><strong>Newer specialized models</strong> (ColPali and similar) embed document page images directly for retrieval and materially outperform both approaches on document search — worth evaluating if that is your corpus.` },

{ q: 'How do you index and query multi-tenant data in a vector database?', tags: ['CRITICAL'], a: `Three patterns, trading isolation against efficiency.<br><br><strong>1. Shared index with tenant metadata filter</strong> — one index, every vector tagged with <code>tenant_id</code>, filtered at query time. Most resource-efficient and simplest to operate, but isolation depends entirely on the filter being applied correctly on every path. A single missing filter is a cross-tenant data leak.<br><br><strong>2. Namespace/partition per tenant</strong> — most vector databases support logical partitions within one deployment. Queries are scoped structurally rather than by predicate, which is both safer and faster (a smaller search space). This is the right default for most SaaS systems.<br><br><strong>3. Separate index or instance per tenant</strong> — strongest isolation, needed for compliance or very large tenants, but it scales poorly in operational cost and wastes memory on many small tenants.<br><br><strong>The recall trap specific to shared indexes:</strong> HNSW traverses a graph built over <em>all</em> vectors. With a selective tenant filter, naive post-filtering discards most traversed candidates and can return far fewer than k results — sometimes zero — while pre-filtering breaks graph connectivity. Confirm your database implements filtered search that maintains recall under high selectivity; this is a real differentiator between engines.<br><br><strong>Non-negotiables:</strong> derive <code>tenant_id</code> from the authenticated session, never from client input or model output; enforce it in a shared data-access layer rather than at each call site; and key any cache by tenant. Also consider the noisy-neighbor problem — one huge tenant degrades latency for everyone in a shared index.` },

{ q: 'What is quantization of embeddings, and how does it reduce storage cost?', tags: ['TRADEOFF'], a: `Quantization stores vectors at reduced numerical precision. Three levels of aggressiveness:<br><br><strong>Scalar quantization (float32 → int8):</strong> map each dimension to 8 bits using per-dimension min/max. 4x compression, typically 1–3% recall loss, and often <em>faster</em> search because more vectors fit in cache. This is close to free and should be the default for any index of meaningful size.<br><br><strong>Binary quantization (1 bit per dimension):</strong> 32x compression, with distance computed by Hamming distance — extremely fast bitwise operations. Recall drops noticeably, so it is used as a first-pass filter with full-precision rescoring of the shortlist.<br><br><strong>Product quantization (PQ):</strong> split the vector into m subvectors, k-means cluster each subspace, and store only centroid ids. 8–32x compression with tunable recall loss. Standard in IVF-PQ for billion-scale indexes.<br><br><strong>The pattern that makes aggressive compression safe is rescoring:</strong> search the compressed index to get a candidate set, then rerank those candidates using full-precision vectors kept on disk. You get compressed-index speed and memory with near-exact recall — this is what makes binary quantization practical rather than a curiosity.<br><br><strong>The discipline:</strong> measure recall@k against an exact brute-force baseline before and after. A 32x saving that quietly drops recall from 0.95 to 0.78 is a bad trade, and nothing in production monitoring will surface it — you only see it as slightly worse answers.` },

{ q: 'How do you benchmark and evaluate embedding model quality?', tags: [], a: `<strong>Use your own data.</strong> MTEB is a shortlist filter — public benchmark rank correlates weakly with performance on a specific corpus, and models are increasingly tuned to those benchmarks. The decision must come from an in-domain eval set.<br><br><strong>Build the eval set:</strong> collect real user queries, and for each label the genuinely relevant documents. 50–200 well-chosen queries beat thousands of synthetic ones. Where you lack labels, bootstrap by having an LLM generate questions from each chunk (the source chunk is the ground-truth positive), then have a human spot-check — this is imperfect but far better than nothing, with the caveat that LLM-generated questions are lexically close to their source and overstate performance.<br><br><strong>Metrics:</strong> recall@k is the ceiling on your RAG system (if the passage is not retrieved, nothing downstream recovers). NDCG@10 captures ranking quality. MRR for single-answer questions. Report per-query-category, because an aggregate hides an entire class failing.<br><br><strong>Measure the operational axes too:</strong> embedding throughput (a full reindex is a real cost), inference latency at your batch size, dimensionality-driven memory, and cost per million tokens for API models.<br><br><strong>Two evaluation mistakes to avoid:</strong> testing retrieval in isolation and assuming end-to-end quality follows — always confirm downstream answer quality moves too; and comparing raw similarity scores across models, which is meaningless since score distributions differ. Compare rankings, not scores.` },

{ q: 'What is the role of metadata in vector databases?', tags: [], a: `Metadata carries everything the vector cannot: source, timestamps, document and chunk ids, permissions, version, language, document type, section headings. It converts pure similarity search into a system that can be correct, secure, and debuggable.<br><br><strong>Four jobs:</strong> <em>filtering</em> (constrain search by date, type, tenant, or permission), <em>security</em> (per-user access control — the only acceptable enforcement point, since prompt instructions cannot enforce authorization), <em>attribution</em> (citations need source, page, and URL), and <em>lifecycle</em> (stable document ids make update and delete single operations rather than search-and-guess).<br><br><strong>The design decision that bites later:</strong> filterable fields must be chosen at ingest, because adding one often requires reindexing. Denormalize aggressively — store the document title, section, and permission set on every chunk even though it duplicates data. Storage is cheap; a reindex is not.<br><br><strong>The performance consideration:</strong> filter selectivity interacts badly with ANN graphs. A highly selective filter under naive post-filtering returns fewer than k results — check whether your engine supports native filtered search that preserves recall.<br><br><strong>Second-order:</strong> denormalized permission metadata goes stale when someone changes teams or a document is reclassified. That turns a stale-data problem into a data-leak problem, so it needs a sync path from your authorization system, not a one-time copy at ingest.` },

{ q: 'How do you handle vector search at billion scale?', tags: ['TRADEOFF'], a: `At a billion vectors, memory is the binding constraint: 1B × 768 dims × 4 bytes ≈ 3TB in float32, before graph overhead. Pure in-memory HNSW is economically out.<br><br><strong>Compress:</strong> IVF-PQ is the standard billion-scale index — coarse quantization into cells plus product quantization of residuals, reaching 8–32x compression. Or <strong>DiskANN</strong>, which keeps a compressed representation in RAM for graph traversal and reads full vectors from SSD only for the final candidates — designed exactly for this regime and delivering high recall at a fraction of the memory.<br><br><strong>Shard:</strong> partition across machines by tenant, date, or semantic cluster. Semantic sharding lets a query search only relevant shards; hash sharding requires scatter-gather across all shards with merged results — simpler but multiplies load. Add replicas for QPS and availability.<br><br><strong>Rescore:</strong> the pattern that keeps compressed indexes accurate — retrieve a wide candidate set from the compressed index, then rerank with full-precision vectors from disk or a separate store.<br><br><strong>Operational realities people underestimate:</strong> index <em>build</em> time becomes a project (hours to days, requiring distributed construction and checkpointing); incremental updates degrade graph quality over time, so periodic rebuilds are necessary; and recall must be monitored continuously against a sampled exact baseline, because at this scale degradation is invisible in every other metric.<br><br><strong>The question worth asking first:</strong> can you avoid it? Partitioning by tenant or time so no single query touches a billion vectors is usually cheaper than engineering a billion-scale index.` },

{ q: 'What is hybrid search?', tags: ['CRITICAL'], a: `Hybrid search runs sparse lexical retrieval (BM25) and dense vector retrieval in parallel, then fuses the ranked lists — most commonly with Reciprocal Rank Fusion, <code>score = Σ 1/(k + rank)</code>, which merges rankings without requiring the two score scales to be comparable.<br><br><strong>Why it consistently wins:</strong> the two methods fail in complementary ways. Dense retrieval captures paraphrase and synonymy but is weak on exact tokens — product SKUs, error codes, function names, acronyms, rare names — because those strings are underrepresented in embedding training and get smeared into nearby regions. BM25 handles exact and rare-term matching with proper IDF weighting but has no notion of synonymy.<br><br>On technical and enterprise corpora dense with identifiers, hybrid often beats dense-only by a wide margin — it is the highest-return change after adding a reranker.<br><br><strong>Implementation notes:</strong> prefer RRF over weighted score fusion, because raw BM25 and cosine scores live on incomparable, corpus-dependent scales and normalizing them correctly is fiddly. RRF's <code>k</code> (typically 60) dampens the influence of top ranks. If you do weight the two channels, tune the weight on your eval set — the right balance is corpus-dependent.<br><br><strong>The current default architecture:</strong> hybrid retrieval to build a wide candidate set, then a cross-encoder reranker for final precision. Either component alone leaves significant quality unclaimed.` },

{ q: 'How do you fine-tune an embedding model for a specific domain?', tags: ['TRADEOFF'], a: `Fine-tuning with contrastive learning on in-domain (query, relevant passage) pairs is the largest available quality gain when general models underperform on your vocabulary.<br><br><strong>Getting training data</strong> is the hard part. Best: real query logs with click or thumbs-up signals as weak positives. Failing that, bootstrap synthetically — have an LLM generate plausible questions for each chunk, treating the source chunk as the positive. This works, with an important caveat: LLM-generated questions are lexically close to their source, so they overstate improvement. Validate on a human-labeled set.<br><br><strong>Hard negatives are what determine quality.</strong> Mine them by retrieving with the current model and taking high-ranked but non-relevant passages. Random negatives are trivially separable and teach the model almost nothing — this is the most common reason a fine-tune underdelivers. Watch for false negatives (a mined "negative" that is actually relevant), which actively harm training.<br><br><strong>Training:</strong> MultipleNegativesRankingLoss with in-batch negatives and a large batch size is the standard recipe (sentence-transformers makes it a short script). A few thousand good pairs usually produce a meaningful lift; low learning rates and few epochs avoid destroying the base model's general capability.<br><br><strong>The costs to weigh:</strong> you commit to a full reindex, to owning and versioning a model artifact, and to re-running the process as the domain shifts. Try hybrid search plus a reranker first — that combination often closes the same gap in days with no reindex, and fine-tuning is the escalation when eval shows it still is not enough.` },

{ q: 'Your vector database uses too much memory. How do you reduce it?', tags: ['TRADEOFF'], a: `Compute the breakdown first: raw vectors are <code>n × dims × 4 bytes</code>, HNSW graph overhead adds roughly <code>n × M × 8 bytes</code> (often 30–50% more), plus payload metadata. Knowing which term dominates tells you which lever matters.<br><br><strong>Quantize (highest return):</strong> int8 scalar quantization gives 4x on vectors for 1–3% recall loss and often speeds up search through better cache locality. Binary quantization gives 32x if paired with full-precision rescoring of the shortlist. This is usually the whole answer.<br><br><strong>Reduce dimensionality:</strong> if using a Matryoshka-trained model, truncate — 3072 → 1024 is frequently near-lossless. Otherwise switch to a smaller model, accepting a reindex.<br><br><strong>Reduce vector count:</strong> deduplicate near-identical chunks (often a surprising share of an enterprise corpus), drop boilerplate, and revisit chunk size — 50% overlap doubles your index for marginal benefit.<br><br><strong>Tune the index:</strong> lower HNSW <code>M</code> to shrink graph overhead at some recall cost, or switch to IVF-PQ or DiskANN if you are at a scale where an in-memory graph is not viable.<br><br><strong>Architectural:</strong> shard by tenant or date so each node holds a subset; move cold partitions to disk-based indexes.<br><br><strong>The non-negotiable:</strong> measure recall@k against an exact baseline before and after every change. Memory savings are visible on a dashboard; the recall you traded for them is not.` },

{ q: 'Your vector database cannot scale to millions of embeddings. How do you fix the bottleneck?', tags: [], a: `Identify the actual bottleneck first — "cannot scale" is usually one specific thing, and millions of vectors is not inherently large (a well-configured single node handles tens of millions).<br><br><strong>If ingestion is the bottleneck:</strong> you are likely embedding serially or upserting one vector at a time. Batch embedding calls, parallelize across workers, and use bulk upsert APIs. Build the index offline and load it, rather than inserting live — incremental HNSW construction is far slower than a batch build.<br><br><strong>If memory is the bottleneck:</strong> quantize to int8, reduce dimensions, or move to a disk-backed index. See the memory-reduction path above.<br><br><strong>If query latency is the bottleneck:</strong> confirm the index is actually being used — a surprisingly common bug is a missing or unbuilt index causing brute-force scans. Then tune <code>ef_search</code>, add replicas for QPS, and pre-filter by metadata to shrink the search space.<br><br><strong>If a selective filter is the problem:</strong> post-filtering with a selective predicate forces massive over-fetching. Switch to native filtered search, or shard by the filter dimension so routing replaces filtering.<br><br><strong>Architecturally:</strong> shard by tenant, date, or domain — this improves latency and relevance simultaneously by shrinking each query's candidate space.<br><br><strong>The question to ask before engineering:</strong> is a dedicated vector database the right tool? pgvector with HNSW handles millions of vectors comfortably alongside relational data, and "we outgrew it" is often a configuration problem rather than a capacity one.` },

{ q: 'Your new embedding model has different dimensions than production vectors. How do you handle the mismatch?', tags: ['CRITICAL'], a: `There is no in-place fix. Different dimensionality means a different vector space — the vectors are not merely differently sized, they are semantically incomparable, so there is nothing to convert. <strong>A full re-embed of the corpus is required.</strong><br><br><strong>The safe migration is blue-green:</strong> stand up a new index (new collection or namespace) with the correct dimension, re-embed the entire corpus into it, evaluate old versus new on a golden query set, then cut over atomically behind a feature flag with a rollback path. Keep the old index until you have confidence in production metrics.<br><br><strong>The failure to avoid absolutely:</strong> writing new-model vectors into the existing index. Most databases will reject a dimension mismatch — but if you "solve" that by padding or truncating to match, you get a half-migrated index mixing incompatible spaces that degrades silently rather than erroring. That is the worst outcome, because retrieval quality drops with no signal pointing at the cause.<br><br><strong>Operational details:</strong> dual-write during migration so newly ingested documents land in both indexes and the new one is not stale at cutover; checkpoint the re-embedding job (it can run for hours and will fail partway); and budget the embedding cost — millions of chunks through an API model is a real bill.<br><br><strong>The lesson for next time:</strong> store the embedding model name and version as index metadata and assert on it at write time. That turns a silent corruption into a loud failure.` },

{ q: 'Vector search returns irrelevant results despite high similarity scores. How do you fix it?', tags: ['CRITICAL'], a: `<strong>The premise is the bug: high cosine similarity does not mean relevant.</strong> Embedding scores are relative, not calibrated — in a normalized space, typical similarities between unrelated documents can sit at 0.7–0.8, so 0.85 sounds high and means little. A fixed threshold copied from a tutorial is meaningless without calibrating on your own corpus.<br><br><strong>Then check likely causes in order:</strong><br>• <em>Missing instruction prefixes</em> — models like E5 and BGE require <code>query:</code>/<code>passage:</code>. Omitting them degrades retrieval silently and is a very common bug.<br>• <em>Metric mismatch</em> — indexing with L2 when the model was trained for cosine, or failing to normalize.<br>• <em>Chunk size too large</em> — a chunk covering five topics produces an averaged vector that is moderately similar to everything and strongly similar to nothing. This is the most common structural cause.<br>• <em>Exact-term queries</em> — identifiers, codes, and rare names are exactly what dense retrieval handles worst. Add BM25 hybrid search.<br>• <em>Negation and conditions</em> — embeddings poorly represent "not," so a passage saying the opposite scores high.<br><br><strong>The highest-return fix is a cross-encoder reranker.</strong> It scores query and document jointly with full attention, capturing the term-level interactions and negation a dot product cannot, and it typically resolves this class of problem outright.<br><br><strong>And calibrate rather than guess:</strong> plot the score distribution for known-relevant versus known-irrelevant pairs on your data to pick a threshold with actual evidence behind it.` },

{ q: 'You deployed a new embedding model and search quality crashed. How do you handle it?', tags: ['CRITICAL'], a: `<strong>Roll back first, diagnose second.</strong> If you deployed blue-green with the old index retained, flip the flag — quality restored in seconds. If you re-embedded in place with no fallback, you now have an incident with no quick recovery, which is the argument for blue-green in the first place.<br><br><strong>Then find the cause, in likelihood order:</strong><br>• <em>Partial migration</em> — some vectors are from the old model and some from the new, so the index mixes incompatible spaces. Check the count of vectors written by the new job against the corpus size.<br>• <em>Query/document asymmetry</em> — queries embedded with the old model (or without the new model's required <code>query:</code> prefix) while documents used the new one. Extremely common, and it produces exactly this symptom.<br>• <em>Missing instruction prefixes</em> on either side.<br>• <em>Metric or normalization mismatch</em> — the new model expects cosine on normalized vectors and the index is configured for L2, or the client stopped normalizing.<br>• <em>Genuinely worse model for your domain</em> — a higher MTEB score does not imply better in-domain performance.<br><br><strong>Verify with a direct test:</strong> take a known query with a known correct document, embed both with the new model, compute similarity manually, and compare against a brute-force scan. That isolates model behavior from index configuration in one step.<br><br><strong>The process fix:</strong> never cut over on benchmark scores. Evaluate the candidate index on a golden query set before switching, and gate the deploy on that result.` },

{ q: 'Semantic search fails for short queries. How do you improve it?', tags: ['TRADEOFF'], a: `Short queries fail because there is little signal to embed. "Refund policy" is two tokens producing a vague vector, and the length asymmetry against a 400-token passage makes the match unstable — this is the asymmetric-retrieval problem in its sharpest form.<br><br><strong>Cheapest effective fix: hybrid search.</strong> Short queries are usually keyword-like, which is exactly BM25's strength — it weights rare terms properly and matches exactly. For a corpus of short queries, adding BM25 often resolves most of the problem with no model change.<br><br><strong>Query expansion:</strong> have an LLM expand the query into a fuller form, or use HyDE — generate a hypothetical answer and embed <em>that</em>, which closes the length and style gap between a two-word query and a passage. Effective, at the cost of an LLM call in the retrieval path.<br><br><strong>Use the right model:</strong> asymmetric retrieval models (E5, BGE with <code>query:</code>/<code>passage:</code> prefixes) are explicitly trained on short-query/long-passage pairs. If you are using a symmetric similarity model, that alone can explain the failure.<br><br><strong>Add context:</strong> in multi-turn settings, rewrite the query against conversation history before retrieval — most short queries are follow-ups that are meaningless standalone.<br><br><strong>And rerank:</strong> a cross-encoder handles short-to-long matching far better than a dot product, so retrieve wide with hybrid and let the reranker sort out precision.<br><br><strong>The tradeoff:</strong> expansion and HyDE add latency and can drift off-topic when the model guesses wrong about an ambiguous short query — measure rather than assuming they help.` },
]};
