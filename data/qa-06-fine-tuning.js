/* Fine-Tuning and Model Adaptation — 26 questions */
window.QA_SECTION = {
  id: '06',
  slug: 'fine-tuning',
  title: 'Fine-Tuning & Model Adaptation',
  blurb: 'LoRA, QLoRA, RLHF, dataset construction, and knowing when weights are the wrong answer.',
  questions: [
{ q: 'What is fine-tuning, and when should you fine-tune an LLM?', tags: ['CRITICAL'], a: `Fine-tuning continues training a pretrained model on task-specific data, updating weights so the behavior you want becomes the default rather than something you elicit with a prompt.<br><br><strong>Fine-tune when the problem is behavioral:</strong> a consistent output format the model keeps deviating from, a domain style or vocabulary, a task-specific reasoning pattern, or an implicit judgment that is hard to articulate in a prompt but easy to demonstrate in examples. Also fine-tune to <em>cut cost</em> — a small tuned model matching a frontier model on a narrow task is often 10–50x cheaper per call, and it removes lengthy few-shot examples from every request.<br><br><strong>Do not fine-tune to inject knowledge.</strong> This is the most common and most expensive mistake. Models trained on facts learn the surface form without reliable recall, hallucinate confidently on near-miss queries, and require a retraining cycle to correct anything. Knowledge that changes belongs in a retrieval store; capability and style belong in weights.<br><br><strong>The sequencing I'd defend:</strong> prompt engineering first (hours, reversible), then RAG if the gap is knowledge, then fine-tuning if the gap is behavior and you have — or can build — a few hundred to a few thousand high-quality examples. Fine-tuning commits you to a model artifact, an eval suite, a retraining path, and a serving story, which is real ongoing cost.` },

{ q: 'Explain the difference between full fine-tuning and PEFT.', tags: ['CRITICAL'], a: `<strong>Full fine-tuning</strong> updates every parameter. Memory is the binding constraint: with Adam you need weights, gradients, and two optimizer states, so roughly 16 bytes per parameter in mixed precision — a 7B model needs ~112GB before activations, which is multi-GPU territory. Each task produces a full model copy.<br><br><strong>PEFT</strong> freezes the base model and trains a small number of new or selected parameters — LoRA adapters, prefix vectors, or adapter layers. Trainable parameters drop by 100–10,000x, and because gradients and optimizer states only exist for those, memory falls dramatically: a 7B LoRA fine-tune fits comfortably on a single 24GB GPU.<br><br><strong>The operational advantage is bigger than the training saving:</strong> adapters are megabytes, not gigabytes. You can store hundreds of task-specific adapters, swap them at runtime, and serve many tasks from one base model in memory. Full fine-tuning gives you one 14GB artifact per task.<br><br><strong>Quality comparison, honestly:</strong> LoRA matches full fine-tuning closely on most instruction-following and style adaptation. The gap appears when the task requires substantial distributional shift — a new language, a genuinely new capability, or continued pretraining on a domain — where the low-rank update cannot express the needed change. In that regime full fine-tuning or continued pretraining wins.<br><br><strong>Default:</strong> LoRA. Escalate to full fine-tuning only when eval shows the low-rank constraint is the binding limitation.` },

{ q: 'What is LoRA, and how does it work?', tags: ['CRITICAL'], a: `LoRA freezes the pretrained weight matrix W and learns a low-rank update alongside it: the forward pass becomes <code>h = Wx + BAx</code>, where A is r×d and B is d×r with r ≪ d (typically 8–64). Only A and B train. A is randomly initialized and B is zero-initialized, so the adapter starts as a no-op and training begins exactly at the pretrained model.<br><br><strong>The hypothesis it rests on:</strong> the weight <em>update</em> needed for adaptation has low intrinsic rank — you are steering an already-capable model, not teaching it from scratch — so a rank-16 update captures most of what full fine-tuning would do. Empirically this holds well for instruction tuning and style adaptation.<br><br><strong>The parameter saving is large:</strong> for a 4096×4096 matrix, full fine-tuning trains 16.8M parameters; rank-16 LoRA trains 131k — about 0.8%.<br><br><strong>The property that matters most in production:</strong> at inference you can <em>merge</em> BA into W, producing a standard model with zero added latency. Or keep adapters separate and hot-swap them per request, serving many tasks from one base model in memory — which is how multi-tenant fine-tuned serving works at all.<br><br><strong>Practical knobs:</strong> rank r (16–32 covers most cases; higher rarely helps and can overfit), alpha (scaling, commonly 2r), and which modules to target — attention projections are standard, adding the MLP layers helps on harder adaptations at higher cost.` },

{ q: 'What is QLoRA, and how does it enable consumer-hardware fine-tuning?', tags: ['CRITICAL'], a: `QLoRA quantizes the frozen base model to 4-bit and trains LoRA adapters in 16-bit on top of it. Because the base is frozen, quantizing it costs no gradient fidelity — you only need it for forward passes — while the adapters, which actually train, stay at full precision.<br><br><strong>Three technical contributions:</strong> <em>NF4</em> (4-bit NormalFloat), a quantization datatype information-theoretically optimal for normally-distributed weights, which neural network weights approximately are. <em>Double quantization</em>, quantizing the quantization constants themselves for a further ~0.4 bits/param saving. And <em>paged optimizers</em>, using unified memory to page optimizer state to CPU during memory spikes, preventing OOM crashes on long sequences.<br><br><strong>The result:</strong> a 65B model fine-tunable on a single 48GB GPU, and a 7B on a 12–16GB consumer card. That collapsed the cost of fine-tuning from a cluster problem to a laptop-adjacent one, and it is why the open fine-tuning ecosystem exists in its current form.<br><br><strong>The tradeoffs to state:</strong> training is slower than 16-bit LoRA because weights are dequantized on the fly for every forward pass — you are trading compute for memory. Quality is close to 16-bit LoRA on most benchmarks but not identical, and the gap widens on tasks sensitive to precision. And merging a 16-bit adapter back into a 4-bit base is lossy, so for serving you typically merge into the original 16-bit weights or keep the adapter separate.` },

{ q: 'How does fine-tuning work mechanically?', tags: [], a: `Same machinery as pretraining, different data and scale. You format examples into the model's chat template (the role markers it was post-trained on — getting this wrong is a common silent failure), tokenize, and run supervised training with cross-entropy loss on next-token prediction.<br><br><strong>The detail that matters most is loss masking.</strong> For instruction tuning you compute loss only on the <em>assistant response</em> tokens, masking the prompt and system message. If you train on the full sequence, the model spends capacity learning to generate user messages, which wastes signal and degrades quality. Most bugs in hand-rolled fine-tuning pipelines are here.<br><br><strong>The loop:</strong> forward pass on a batch → cross-entropy against the target tokens → backprop → optimizer step (AdamW, low learning rate — typically 1e-5 to 5e-5 for full fine-tuning, 1e-4 to 3e-4 for LoRA) → repeat, with a warmup and a cosine or linear decay schedule.<br><br><strong>Why the learning rate is so much lower than pretraining:</strong> you are nudging an already-competent model. High learning rates destroy pretrained capability — catastrophic forgetting shows up as a model that follows your format perfectly and can no longer do anything else.<br><br><strong>Practical mechanics:</strong> pack or pad sequences to a fixed length; use gradient accumulation to reach an effective batch size your GPU cannot hold directly; gradient checkpointing to trade compute for activation memory; and evaluate on a held-out set every few hundred steps, because 2–3 epochs is often past the point of overfitting on small datasets.` },

{ q: 'Explain prefix tuning and prompt tuning. How do they differ from LoRA?', tags: [], a: `<strong>Prompt tuning</strong> learns a small set of continuous vectors prepended to the input embeddings — "soft prompt" tokens that correspond to no real vocabulary entry, optimized by gradient descent while the model stays frozen. <strong>Prefix tuning</strong> generalizes this: instead of only the input layer, it prepends learned key-value vectors at <em>every</em> attention layer, giving far more capacity.<br><br><strong>How they differ from LoRA structurally:</strong> LoRA modifies the weight matrices (an additive low-rank update to W), so it changes the computation itself. Prefix and prompt tuning leave weights untouched and instead add learned <em>inputs</em> the model attends to — steering through context rather than through parameters.<br><br><strong>The practical consequences:</strong> LoRA can be merged into the base weights for zero inference overhead; prefix/prompt tuning always consumes context positions at inference, so it costs sequence length forever. Prompt tuning is extremely parameter-efficient (thousands of parameters) but needs large models to work well — it underperforms badly below ~10B. Prefix tuning is more capable but the optimization is notoriously unstable.<br><br><strong>Why LoRA won:</strong> better quality, no inference-time cost after merging, stable training, and a mature tooling ecosystem. Prompt tuning retains a niche where you want to serve thousands of tasks from one model with truly minimal storage, and prefix tuning is mostly of historical interest.<br><br>Both also require access to embeddings or internal activations, so neither is available through most closed APIs.` },

{ q: 'What is adapter-based fine-tuning?', tags: [], a: `Adapters (Houlsby-style) insert small trainable bottleneck modules <em>inside</em> each Transformer block — typically down-project to a small dimension, apply a nonlinearity, up-project back, with a residual connection. The base model freezes; only the adapter modules train, usually 0.5–5% of parameters.<br><br>This was the original PEFT approach and it established the core idea: adapt a frozen model by training a small number of inserted parameters, and store one small artifact per task.<br><br><strong>Why LoRA largely displaced it:</strong> adapters add layers to the computation graph, so they impose <em>inference latency</em> — extra sequential operations per block that cannot be removed. LoRA's update is a parallel low-rank term that can be merged into the existing weight matrix, giving identical inference cost to the base model. For a serving-cost-sensitive deployment that difference is decisive.<br><br><strong>Where the adapter lineage still matters:</strong> the concept generalizes. <em>AdapterFusion</em> composes multiple task adapters; <em>(IA)³</em> learns per-channel scaling vectors, which is even more parameter-efficient than LoRA; and the modularity idea — swappable capability units over a shared frozen base — is the same idea underlying LoRA serving and multi-adapter systems.<br><br><strong>The framing worth giving:</strong> adapters, LoRA, prefix tuning, and (IA)³ are all points on one design space — where you inject trainable capacity and whether it can be folded back into the base computation. LoRA occupies the best point on that tradeoff for most current workloads.` },

{ q: 'What is RLHF, and how is it used to align LLMs?', tags: ['CRITICAL'], a: `RLHF aligns a model to human preferences in three stages. <strong>1. SFT:</strong> supervised fine-tuning on high-quality demonstrations, producing a model that follows instructions in the right format. <strong>2. Reward modeling:</strong> collect human comparisons of response pairs and train a reward model with a Bradley-Terry objective to predict which response a human would prefer. <strong>3. RL:</strong> optimize the policy against that reward model with PPO, plus a KL penalty against the SFT reference model to prevent drifting off-distribution.<br><br><strong>Why comparisons rather than direct ratings:</strong> humans are far more reliable at relative judgments ("A is better than B") than at absolute scores, where annotator calibration drifts and disagreement is high. This is the key design insight.<br><br><strong>Why RL rather than more supervised learning:</strong> for open-ended generation there is no single correct output to supervise against, but there is a preference signal over sampled outputs. RL lets the model optimize a scalar reward over its own generations — learning from its own distribution rather than imitating a fixed set.<br><br><strong>The known failure modes:</strong> <em>reward hacking</em> — the policy exploits proxy artifacts (length, confident tone, sycophancy) that score well without being better; <em>alignment tax</em> — measurable capability loss on hard tasks, and over-refusal on benign prompts; and cost, since it requires a large human annotation pipeline plus four models in memory during PPO.<br><br><strong>Where the field moved:</strong> DPO removes the reward model and RL loop entirely at comparable quality, which is why most open alignment now uses it. Online RL retains an edge where rewards are verifiable — math and code.` },

{ q: 'What is instruction tuning, and why is it important for chat models?', tags: ['FREQUENTLY ASKED'], a: `Instruction tuning is supervised fine-tuning on a diverse set of (instruction, response) pairs spanning many task types, teaching the model to interpret a request and respond to it rather than merely continue text.<br><br><strong>The problem it solves:</strong> a base pretrained model is a text <em>completer</em>. Given "Explain photosynthesis," a plausible continuation is another exam question, or a table of contents — because that is what follows such a string in web text. It has the knowledge; it does not have the behavior of answering.<br><br><strong>What makes it work is task diversity, not volume.</strong> The critical finding (FLAN, T0, and later LIMA) is that training across many varied task types produces <em>generalization to unseen instructions</em> — the model learns "follow the request" as an abstraction rather than memorizing specific tasks. LIMA showed 1,000 carefully curated examples can outperform 50,000 mediocre ones, which reframes this as a data-quality problem rather than a scale problem.<br><br><strong>It also establishes the chat template</strong> — the role markers and special tokens delimiting system, user, and assistant turns. This is why using the wrong template at inference degrades a model badly, and why it is one of the most common silent bugs in self-hosted deployments.<br><br><strong>Relationship to alignment:</strong> instruction tuning teaches <em>capability</em> to follow instructions; RLHF/DPO then tunes <em>which</em> responses are preferred among the ones it can produce. SFT is necessary and comes first; preference optimization refines.` },

{ q: 'How do you prepare a dataset for fine-tuning?', tags: ['CRITICAL'], a: `<strong>Quality dominates quantity.</strong> LIMA-style results show a few thousand carefully curated examples beat tens of thousands of noisy ones — and noisy examples actively teach wrong behavior, so bad data is worse than no data. Start at 500–2,000 excellent examples for a focused task.<br><br><strong>Construction requirements:</strong> match the <em>exact</em> format you will use at inference, including the chat template and system prompt — a mismatch between training and serving format is the most common cause of a fine-tune that "did nothing." Cover the real input distribution including edge cases, not just the happy path. Balance across categories, since skew becomes bias. And include examples of correct <em>refusal</em> or abstention if you want that behavior; a model never shown "I don't know" will not produce it.<br><br><strong>Process:</strong> deduplicate aggressively (duplicates drive memorization); hold out a genuine test set <em>before</em> you start iterating, and never tune against it; validate that every example is actually correct — a human pass over the data is the highest-return hour you will spend.<br><br><strong>Sourcing:</strong> production logs with human-verified outputs are ideal because they are distributionally correct. Synthetic generation from a stronger model works and is standard practice, but check the licensing — many providers prohibit training competing models on their outputs — and expect synthetic data to be less diverse than it looks, so filter for variety.<br><br><strong>Loss masking:</strong> compute loss only on assistant tokens. Getting this wrong silently halves your effective signal.` },

{ q: 'What is catastrophic forgetting, and how do you prevent it?', tags: ['TRADEOFF'], a: `Catastrophic forgetting is the loss of previously-learned capability when training on new data. Gradient updates optimizing the new objective overwrite weights encoding old behavior, so a model fine-tuned on your domain gets measurably worse at everything else — including general reasoning and instruction following.<br><br><strong>Why it happens:</strong> nothing in the loss function preserves prior capability. The optimizer moves weights to minimize loss on the data in front of it, and pretrained knowledge is distributed across exactly those weights.<br><br><strong>Mitigations, most practical first:</strong><br>• <em>Use PEFT.</em> LoRA freezes the base model entirely, so pretrained weights cannot be overwritten — this is the single most effective structural defense and a major reason LoRA is the default.<br>• <em>Lower learning rate and fewer epochs.</em> Most forgetting comes from over-training; 1–3 epochs at 1e-5 to 2e-4 is usually sufficient.<br>• <em>Replay / data mixing.</em> Blend 5–20% general instruction data into your domain set so the objective includes preserving general behavior.<br>• <em>Regularization</em> (EWC, L2-to-init) penalizes movement in weights important for prior tasks — principled but rarely used in practice because the simpler options suffice.<br><br><strong>The part teams skip:</strong> you cannot manage what you do not measure. Run general-capability benchmarks (MMLU, instruction-following, and a few of your own non-domain tasks) <em>before and after</em> every fine-tune. Forgetting is invisible until a user hits the forgotten capability, and by then it is a production incident.` },

{ q: 'When should you choose fine-tuning over RAG over prompt engineering?', tags: ['CRITICAL'], a: `Escalate by cost and reversibility, and let the failure mode pick the tool.<br><br><strong>Prompt engineering first.</strong> Hours to iterate, instantly reversible, no infrastructure. Solves more than people expect — most "we need to fine-tune" conclusions come from teams who never built an eval set and never systematically iterated on the prompt.<br><br><strong>RAG when the gap is knowledge.</strong> The model does not <em>know</em> something: private data, current events, anything past the cutoff, anything that changes. RAG gives instant updates, citations, real deletion, and per-user access control — none of which weights provide.<br><br><strong>Fine-tuning when the gap is behavior.</strong> The model knows what to do and will not do it consistently: format adherence, domain style, a task-specific reasoning pattern, or judgment that is easier to demonstrate than describe. Also fine-tune purely for cost — a tuned small model replacing a frontier model on a narrow high-volume task.<br><br><strong>The diagnostic question:</strong> if you put the right document in the prompt, does the model answer correctly? Yes → it is a knowledge problem, use RAG. No → it is a behavior problem, and fine-tuning is on the table.<br><br><strong>They compose.</strong> The strongest production systems fine-tune a small model to follow format and use retrieved context faithfully, then feed it RAG results — domain behavior plus current facts at a fraction of frontier-model cost.<br><br><strong>The mistake to name:</strong> fine-tuning to inject facts. It produces unreliable recall, confident hallucination on near-miss queries, and a retraining cycle for every correction.` },

{ q: 'How do you evaluate a fine-tuned model?', tags: ['CRITICAL'], a: `Evaluate on three axes, because improving one while silently destroying another is the normal outcome.<br><br><strong>1. Target task performance</strong> on a held-out set you never tuned against. Use task-appropriate metrics — exact match or F1 for extraction and classification, schema-validity rate for structured output, a rubric-scored LLM judge (validated against human labels first) for open-ended generation. Always compare against the pre-fine-tuning baseline <em>and</em> against a well-prompted frontier model; a fine-tune that underperforms good prompting is a negative result worth knowing.<br><br><strong>2. Retained general capability.</strong> Run MMLU or similar plus a handful of off-domain tasks you care about, before and after. This is the catastrophic-forgetting check, and it is the axis teams routinely skip — then discover in production that the model got great at their format and lost the ability to reason.<br><br><strong>3. Safety and behavior regressions:</strong> refusal rate on harmful prompts, over-refusal on benign ones, and output-format stability.<br><br><strong>Process requirements:</strong> the test set must be held out before you start iterating — iterating against it overfits your hyperparameters to it. Check for contamination between training and test data, especially with synthetically generated sets where near-duplicates are common. And measure operational properties too: latency, cost per call, and output length distribution.<br><br><strong>Then validate in production</strong> with a shadow deployment or A/B, because offline eval sets systematically under-represent the messy tail of real inputs.` },

{ q: 'What is synthetic data generation, and how do you use it for fine-tuning?', tags: ['TRADEOFF'], a: `Synthetic data generation uses a strong model to produce training examples — instructions, responses, or both — when human-labeled data is unavailable or too expensive. Self-Instruct, Evol-Instruct, and most modern instruction datasets work this way.<br><br><strong>Common patterns:</strong> seed with a small set of human examples and prompt a strong model to generate variations; generate questions <em>from</em> your documents (the source passage is the ground-truth answer, which is how RAG eval sets and embedding training pairs get built); or use rejection sampling — generate many candidate responses, keep only those that pass a verifier (tests pass, answer checks, schema validates), which is the highest-quality variant because correctness is enforced rather than assumed.<br><br><strong>The failure modes to name:</strong> <em>limited diversity</em> — synthetic data clusters tightly around the generator's modes and looks more varied than it is, so aggressive dedup and diversity filtering matter. <em>Inherited errors and biases</em> — you are distilling the teacher's mistakes along with its capability. And <em>distribution mismatch</em> — LLM-generated questions are lexically close to their source passages, which inflates eval scores relative to real user queries.<br><br><strong>Non-negotiables:</strong> filter with a verifier or a judge, human-review a sample before training, and always validate on <em>real</em> held-out data. Also check licensing — most API providers prohibit using outputs to train competing models, which is a legal question, not a technical one.<br><br><strong>Where it works best:</strong> augmenting a small real dataset, generating edge cases you would not think of, and any domain with a programmatic verifier.` },

{ q: 'What are the key hyperparameters for fine-tuning?', tags: [], a: `<strong>Learning rate</strong> is the one that matters most. Full fine-tuning: 1e-5 to 5e-5. LoRA: 1e-4 to 3e-4 (higher, because fewer parameters absorb the update). Too high destroys pretrained capability — catastrophic forgetting shows up here first; too low and nothing changes. Use warmup (3–10% of steps) plus cosine decay.<br><br><strong>Epochs:</strong> 1–3 for most fine-tunes. Small datasets overfit fast — memorization typically appears by epoch 3–4, so evaluate on a held-out set every few hundred steps and early-stop rather than trusting a fixed count.<br><br><strong>Effective batch size:</strong> 16–128 sequences via gradient accumulation. Larger batches give more stable gradients; below ~8 training is noisy. Scale learning rate roughly with batch size.<br><br><strong>LoRA-specific:</strong> <em>rank r</em> — 8–16 for style and format adaptation, 32–64 for harder domain shifts; higher rank rarely helps and increases overfitting risk. <em>alpha</em> — commonly 2r, since the effective scaling is alpha/r. <em>Target modules</em> — attention projections (q, k, v, o) as standard; adding MLP layers helps on harder tasks at higher cost. <em>Dropout</em> — 0.05–0.1 on small datasets.<br><br><strong>Also:</strong> max sequence length (drives memory quadratically through attention), and weight decay (0.0–0.1).<br><br><strong>The practical advice:</strong> learning rate and epochs account for most of the variance. Sweep those two first on a small subset before touching anything else — rank tuning is usually a distraction relative to fixing your data.` },

{ q: 'How do you fine-tune a model for a specific domain (legal, medical, finance)?', tags: ['TRADEOFF'], a: `<strong>First decide whether fine-tuning is the right tool.</strong> If the requirement is current, citable, auditable facts — case law, drug interactions, filings — that is RAG, not fine-tuning. Regulated domains typically need attribution and deletion, which weights cannot provide. Fine-tune for domain <em>reasoning patterns, terminology, and output conventions</em>; retrieve the facts.<br><br><strong>The staged recipe when the gap is genuinely behavioral:</strong> optionally <em>continued pretraining</em> on a large unlabeled domain corpus (billions of tokens) to shift vocabulary and style, then <em>supervised fine-tuning</em> on task-specific examples in your exact inference format. Most teams should skip the first stage — it is expensive and often unnecessary if the base model has reasonable domain exposure.<br><br><strong>Domain-specific requirements that dominate the ML work:</strong> expert-labeled data is expensive and non-negotiable in high-stakes domains — you cannot crowdsource medical correctness. Handle PHI/PII before training, since a model can memorize and reproduce it, and you cannot delete a fact from weights. Check tokenization, because domain terms often fragment badly. And build evals with domain experts, not automated metrics — a fluent, well-formatted, clinically wrong answer scores well on ROUGE.<br><br><strong>The safety obligation:</strong> in medicine, law, and finance the failure cost is asymmetric — a confident wrong answer is far worse than a refusal. Train and evaluate explicitly for calibrated abstention, keep a human in the loop for consequential outputs, and measure the confidently-wrong rate as a first-class metric, not accuracy alone.` },

{ q: 'What is continual pre-training, and when would you use it?', tags: ['TRADEOFF'], a: `Continual (or continued) pretraining runs the original self-supervised objective — next-token prediction — on a large unlabeled domain corpus, before any supervised fine-tuning. It is not instruction tuning; there are no input/output pairs, just raw domain text.<br><br><strong>When it is justified:</strong> the domain has genuinely different language statistics that the base model saw little of — clinical notes, legal filings, low-resource languages, specialized code, proprietary internal documentation with its own vocabulary and conventions. The signal that you need it: the base model's perplexity on your domain text is high, or it fundamentally misreads domain terminology rather than merely formatting answers wrong.<br><br><strong>Scale requirements are the reason most teams should skip it:</strong> it typically takes billions of tokens to move the model meaningfully, which means real compute and a real data pipeline. Below that scale you get forgetting without much domain gain — the worst of both.<br><br><strong>Managing forgetting is the central risk.</strong> Mix 5–20% general-corpus data into the domain data (replay), use a lower learning rate than pretraining, and evaluate general benchmarks throughout. A model that becomes fluent in your domain and loses instruction-following is a net regression.<br><br><strong>The sequencing:</strong> continued pretraining → SFT on task data → optional preference tuning. Skipping SFT afterward leaves you with a domain-fluent base model that does not follow instructions, which is rarely what you wanted.<br><br><strong>The default recommendation:</strong> try RAG and SFT first. Continued pretraining is the escalation when eval proves the base model's domain representation is the binding constraint.` },

{ q: 'How do you merge multiple LoRA adapters?', tags: ['TRADEOFF'], a: `Because LoRA updates are additive low-rank matrices, they can be combined arithmetically. <strong>Linear merging</strong> is the simplest: <code>W + Σ wᵢ·BᵢAᵢ</code> with per-adapter weights. <strong>Concatenation</strong> stacks the low-rank factors, preserving each adapter's subspace at the cost of a higher effective rank. <strong>TIES-merging</strong> and <strong>DARE</strong> address interference explicitly — trimming small-magnitude parameters, resolving sign conflicts, and rescaling — which measurably outperforms naive averaging when adapters conflict.<br><br><strong>Why you would want to:</strong> compose capabilities without retraining — a style adapter plus a domain adapter — or serve one merged model instead of swapping adapters per request.<br><br><strong>Why it often disappoints:</strong> adapters trained independently occupy overlapping parameter directions, and naive averaging produces destructive interference where both capabilities degrade rather than combine. The more the tasks conflict, the worse it gets, and the result is unpredictable enough that it must be evaluated empirically per combination — there is no reliable a priori guarantee.<br><br><strong>The alternatives worth preferring:</strong> <em>adapter routing</em> — keep adapters separate and select per request, which avoids interference entirely and is well supported by modern serving stacks (S-LoRA, vLLM multi-LoRA). Or simply <em>train one adapter on the combined data</em>, which is usually higher quality than merging two and is the honest baseline any merging approach should be measured against.<br><br><strong>Practical requirement:</strong> adapters must share the same base model and, for most methods, compatible target modules and rank.` },

{ q: 'What is the difference between SFT and alignment training?', tags: [], a: `<strong>SFT (supervised fine-tuning)</strong> is imitation: given a prompt, maximize the likelihood of a demonstrated response. It teaches the model <em>what a good response looks like</em> and establishes format, task capability, and instruction-following.<br><br><strong>Alignment training</strong> (RLHF, DPO, and variants) optimizes over <em>preferences between</em> responses rather than imitating a single target. It teaches which of several plausible responses humans actually prefer.<br><br><strong>Why both are needed:</strong> SFT can only teach what is demonstrated, and for open-ended generation there is no single correct output to demonstrate. It also has a structural limitation — it only ever sees positive examples, so it never learns what <em>not</em> to do. Preference optimization uses negative signal directly: this response was worse, move away from it. That is how you get helpfulness, tone, safety behavior, and reduced sycophancy, none of which are cleanly specifiable as demonstrations.<br><br><strong>The pipeline order is fixed:</strong> pretraining → SFT → preference optimization. Alignment on a non-SFT'd base model does not work well, because preference optimization refines an existing distribution rather than creating capability. SFT gets the model into the right region; alignment picks within it.<br><br><strong>The tradeoff alignment introduces:</strong> the alignment tax — measurable capability loss and over-refusal, controlled by the KL penalty against the SFT reference. That is a dial, not a bug, and you should measure both sides of it: safety metrics <em>and</em> false-refusal rate on benign prompts.` },

{ q: 'What is RLAIF, and how does it differ from RLHF?', tags: [], a: `RLAIF replaces the human preference labels in RLHF with labels generated by an AI model. The pipeline is otherwise identical: collect response pairs, get preference judgments (from an LLM rather than annotators), train a reward model, optimize the policy — or skip the reward model and use the AI preferences directly with DPO.<br><br><strong>Constitutional AI</strong> is the influential instantiation: the labeling model judges responses against an explicit written set of principles (a "constitution"), and can also critique and revise its own outputs before preference collection. The principles are human-authored, so human values enter through the constitution rather than through per-example labels.<br><br><strong>Why it matters:</strong> human preference collection is the bottleneck in RLHF — expensive, slow, inconsistent between annotators, and hard to scale to the volume RL needs. AI labeling is orders of magnitude cheaper, instantly scalable, and perfectly consistent. Google's RLAIF work found it matches RLHF on summarization and dialogue helpfulness.<br><br><strong>The limitations to state:</strong> you inherit the labeler model's biases and blind spots, and it cannot exceed the labeler on dimensions where the labeler is itself wrong. There is a circularity risk — using a model to align models can entrench shared failure modes across the ecosystem. And on genuinely subjective or culturally-specific judgments, an AI labeler's "preference" is not a substitute for the population you actually serve.<br><br><strong>Practical stance:</strong> hybrid. AI labeling for volume and consistency, human labeling for high-stakes categories, safety-critical behavior, and periodic validation that the AI labeler still agrees with humans.` },

{ q: 'What is knowledge distillation for fine-tuning, and what are the legal considerations?', tags: ['TRADEOFF'], a: `Distillation trains a small student model to imitate a larger teacher. For LLMs the practical form is <strong>sequence-level distillation</strong>: generate a large corpus of teacher outputs — ideally including reasoning traces — and supervise-fine-tune the student on them. Classic logit-matching (soft targets) transfers more information per example but requires access to the teacher's full output distribution, which closed APIs do not expose.<br><br><strong>Quality technique:</strong> rejection sampling — generate several teacher outputs per prompt and keep only those passing a verifier — filters teacher errors and improves the student substantially over naive imitation. Chain-of-thought distillation (training on the reasoning, not just the answer) materially outperforms answer-only supervision on reasoning tasks.<br><br><strong>The capability limit, stated honestly:</strong> distillation transfers behavior <em>on the covered distribution</em>, not capability. Students match teachers on well-represented tasks and fall off sharply on hard multi-step reasoning and out-of-distribution inputs — imitating traces is not the same as acquiring the ability to compose steps.<br><br><strong>The legal considerations are real and frequently ignored:</strong> most commercial API providers' terms explicitly prohibit using their outputs to train competing models. That is a contractual restriction independent of copyright. Separately, the copyright status of model outputs is unsettled across jurisdictions, and if the teacher's outputs contain memorized training data you may be propagating third-party content. Open-weight models with permissive licenses (check each — many carry usage restrictions despite being called "open source") avoid the contractual issue entirely.<br><br><strong>The practical advice:</strong> read the terms of service before building on this, and get legal review if the resulting model is commercial.` },

{ q: 'Your fine-tuned model produces factually wrong outputs from bad training data. How do you fix it?', tags: ['CRITICAL'], a: `<strong>Stop and audit the data</strong> — you cannot fix a data problem with hyperparameters. Sample 100–200 training examples and have a domain expert verify correctness. The rate you find is roughly the rate you trained into the model, and teams are consistently surprised by how high it is.<br><br><strong>Then fix systematically:</strong> deduplicate (duplicated errors get learned hardest); filter with a verifier where the domain allows programmatic checking; use an LLM judge to flag suspect examples for human review, which makes a large corpus reviewable; and check provenance — synthetic data generated by a weaker model, or scraped data with no quality gate, are the usual sources.<br><br><strong>Retrain, do not patch.</strong> Adding correct examples on top of wrong ones does not reliably overwrite the learned error, particularly if the wrong pattern was reinforced across many examples. Clean the dataset and retrain from the base model.<br><br><strong>The architectural question worth raising:</strong> if the errors are <em>factual</em>, fine-tuning was probably the wrong mechanism. Facts belong in a retrieval store where they can be corrected in seconds, audited, and cited — not baked into weights where every correction is a training run and there is no attribution. A model fine-tuned for behavior plus RAG for facts fixes this class of problem structurally rather than repeatedly.<br><br><strong>Prevention:</strong> a held-out expert-verified eval set that gates every training run, and provenance tracking on training data so you can trace a bad behavior back to its source examples.` },

{ q: 'LoRA or full fine-tuning for a domain assistant? How do you decide?', tags: ['TRADEOFF'], a: `<strong>Default to LoRA and make full fine-tuning prove it is needed.</strong> LoRA matches full fine-tuning on most instruction-following, format, and style adaptation while costing a fraction of the memory, producing megabyte-sized artifacts, and structurally preventing catastrophic forgetting since base weights are frozen.<br><br><strong>Decide empirically:</strong> run LoRA at rank 16 and rank 64 first. If quality saturates and still falls short of your target, that is evidence the low-rank constraint is binding and full fine-tuning may help. If rank 16 and 64 perform the same, rank is not your limitation — your data is, and full fine-tuning will not fix it either. This one experiment resolves the question cheaply and most teams skip it.<br><br><strong>Choose full fine-tuning when</strong> the adaptation requires genuine distributional shift — a new language, a substantially different modality of text, or continued pretraining on a domain corpus — where the update needed is not low-rank. Also when you are training on a very large dataset (tens of thousands of examples), where LoRA's capacity limit starts to bind.<br><br><strong>Choose LoRA when</strong> you need multiple task variants (one base model, many swappable adapters — the operational win is large), when GPU budget is constrained, when you want easy rollback, or when preserving general capability matters.<br><br><strong>The practical asymmetry:</strong> LoRA is cheap to try and easy to reverse; full fine-tuning costs 10x the compute, produces a full model artifact per iteration, and risks forgetting. Start where the downside is small.` },

{ q: 'Your fine-tuned model memorized training data instead of learning patterns. How do you fix overfitting?', tags: [], a: `<strong>Confirm it first:</strong> the signature is training loss falling while held-out loss rises, plus verbatim reproduction of training examples when prompted with near-matches. If you have no held-out set, you cannot diagnose this at all — that is the first fix.<br><br><strong>Reduce training pressure:</strong> fewer epochs (1–2, not 5 — most memorization appears after epoch 3), lower learning rate, and early stopping on validation loss rather than a fixed schedule. This resolves the majority of cases.<br><br><strong>Reduce model capacity relative to data:</strong> with LoRA, lower the rank — rank 64 on 500 examples has ample capacity to memorize; rank 8–16 forces generalization. Add LoRA dropout (0.05–0.1) and weight decay.<br><br><strong>Fix the data, which is usually the real cause:</strong> deduplicate aggressively, since duplicated examples are memorized preferentially and near-duplicates from synthetic generation are common. Increase diversity — memorization is what happens when the dataset is too small or too homogeneous for the capacity you gave it. Augment or generate more varied examples covering the actual input distribution.<br><br><strong>Check for contamination</strong> between train and test sets, especially with synthetic data where paraphrases straddle the split and make overfitting look like success.<br><br><strong>The second-order concern:</strong> memorization is also a privacy and compliance problem, not just a quality one. If training data contained PII or proprietary content, a memorizing model can reproduce it in production — which is an argument for scrubbing data before training and for keeping sensitive information in a retrieval store rather than in weights.` },

{ q: 'Your fine-tuned model forgot its general capabilities. How do you fix catastrophic forgetting?', tags: ['CRITICAL'], a: `<strong>Quantify it first</strong> — run general benchmarks (MMLU, instruction-following, a few off-domain tasks you care about) on the base model and the fine-tune. You need the size and shape of the regression before choosing a fix, and often the "forgetting" is narrower than it feels.<br><br><strong>Fixes, in order of practicality:</strong><br>• <em>Switch to LoRA if you did full fine-tuning.</em> Frozen base weights cannot be overwritten — this is the strongest structural defense and usually resolves the problem outright.<br>• <em>Lower the learning rate and cut epochs.</em> Most forgetting is over-training. Dropping from 5e-5 to 1e-5 and from 5 epochs to 2 often preserves general capability at nearly the same task performance.<br>• <em>Replay / data mixing.</em> Blend 5–20% general instruction data (or original-distribution data) into your training set so the objective includes maintaining general behavior. Simple and effective.<br>• <em>Regularization</em> — EWC or L2-to-init penalizes movement in weights important for prior tasks. Principled, rarely necessary given the above.<br><br><strong>The architectural alternative:</strong> keep the adaptation in a swappable adapter and route — general queries to the base model, domain queries to the adapted one. This eliminates the tradeoff rather than balancing it, at the cost of a routing step.<br><br><strong>The process fix that prevents recurrence:</strong> add general-capability benchmarks to your training pipeline as a gate. Forgetting is invisible in domain metrics — the model looks better on everything you measure and worse on everything you do not.` },

{ q: 'Your RLHF preference data has low annotator agreement. How do you ensure quality?', tags: ['TRADEOFF'], a: `<strong>First determine whether the disagreement is noise or genuine subjectivity</strong> — they need opposite responses, and conflating them is the core mistake.<br><br><strong>Diagnose:</strong> measure inter-annotator agreement (Cohen's/Fleiss' kappa) and, critically, break it down by <em>category</em>. Agreement is usually fine on clear quality differences and collapses on genuinely subjective dimensions like tone or creative preference. Also check whether disagreement concentrates in specific annotators (a training or calibration problem) or is uniform (a task-definition problem).<br><br><strong>If it is noise, fix the process:</strong> sharpen the guidelines with concrete criteria and worked examples of edge cases — vague instructions like "pick the better response" guarantee disagreement; run calibration sessions where annotators discuss disagreements; use gold-standard test items to identify and retrain low-quality annotators; and simplify the task — pairwise comparison is far more reliable than absolute ratings, and per-dimension comparisons (which is more helpful? more accurate?) beat a single overall judgment.<br><br><strong>If it is genuine subjectivity:</strong> collect multiple annotations per pair and use soft labels or confidence weighting rather than forcing a majority vote. Filter out pairs where annotators are near-random — these contribute pure noise to the reward model and actively degrade it. And accept that on some dimensions there is no single correct preference; that is a signal to define the target behavior explicitly rather than hoping annotators converge.<br><br><strong>The second-order point:</strong> low agreement puts a ceiling on reward model quality, which puts a ceiling on the aligned policy. It is worth fixing the annotation pipeline before spending compute on RL — no amount of optimization recovers a noisy reward signal.` },
]};
