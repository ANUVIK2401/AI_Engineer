/* AI Infrastructure and Scalability — 25 questions */
window.QA_SECTION = {
  id: '12',
  slug: 'infrastructure',
  title: 'AI Infrastructure & Scalability',
  blurb: 'GPUs, parallelism, batching, speculative decoding, KV cache management, and serving economics.',
  questions: [
{ q: 'How do you improve inference speed in production LLM deployments?', tags: ['CRITICAL'], a: `<strong>Start from the two-phase structure, because the fixes are different.</strong> Prefill is compute-bound and sets TTFT; decode is memory-bandwidth-bound and sets tokens/sec. Optimizing the wrong one buys nothing.<br><br><strong>For TTFT:</strong> prompt caching (reuse the KV cache for a shared prefix — up to ~90% input cost reduction and a large TTFT drop), shorter prompts (rerank and cut k rather than stuffing context), and chunked prefill so long prompts do not stall other requests.<br><br><strong>For throughput and inter-token latency:</strong><br>• <em>Continuous batching</em> — admit and evict sequences at each decode step. The single largest throughput win, roughly an order of magnitude over naive generation.<br>• <em>Quantization</em> — decode is bandwidth-bound, so halving bytes read per token nearly doubles decode speed. int8 is essentially lossless; fp8 on H100+ is near-lossless and hardware-native.<br>• <em>GQA/MQA</em> — a 4–8x smaller KV cache means fewer bytes read per step <em>and</em> more concurrent sequences in memory.<br>• <em>PagedAttention</em> — eliminates cache fragmentation, raising achievable batch size.<br>• <em>Speculative decoding</em> — a small draft model proposes several tokens, the large model verifies them in one parallel pass. 2–3x speedup on the latency-critical path with identical output distribution.<br><br><strong>Architectural levers:</strong> route easy queries to a smaller model, and stream so <em>perceived</em> latency drops even when total time does not.<br><br><strong>Measure before optimizing:</strong> profile TTFT versus generation time. Most "it's slow" reports resolve to one of the two, and guessing wastes a cycle.` },

{ q: 'What are the main LLM optimization techniques?', tags: [], a: `<strong>Group them by what they attack.</strong><br><br><strong>Model size / memory bandwidth:</strong> quantization (int8/int4/fp8 weights, and separately the KV cache), pruning, and distillation into a smaller model. Since decode is bandwidth-bound, these are latency wins, not just memory wins.<br><br><strong>Attention cost:</strong> Flash Attention (exact, IO-aware — makes memory O(n) instead of O(n²) with identical numerics; a default, not a tradeoff), GQA/MQA (shrink the KV cache by sharing K/V heads), sliding-window and sparse attention (approximate, for very long context).<br><br><strong>Serving efficiency:</strong> continuous batching, PagedAttention, chunked prefill, and prefix/prompt caching. These typically deliver the largest end-to-end wins and require no model changes — which is why a purpose-built inference server beats naive generation by roughly 10x.<br><br><strong>Decoding:</strong> speculative decoding (draft-then-verify, 2–3x with identical output distribution), and Medusa-style multi-head prediction.<br><br><strong>System-level:</strong> model routing by query difficulty, response and semantic caching, and disaggregated prefill/decode pools.<br><br><strong>The ordering I would apply:</strong> serving-layer wins first (continuous batching, paged attention, prompt caching) because they are large, safe, and require no quality tradeoff. Then quantization with an eval gate. Then architectural choices (GQA) if you control the model. Then speculative decoding. Routing and caching sit above all of it and often beat every kernel-level optimization combined.<br><br><strong>Always gate on an eval set</strong> — most of these trade quality silently.` },

{ q: 'How do you select GPUs for LLM inference?', tags: ['TRADEOFF'], a: `<strong>Memory capacity is the first filter, and it is a hard gate.</strong> Weights alone: roughly 2 bytes/parameter at fp16, 1 at int8, 0.5 at int4. A 70B model is ~140GB at fp16, ~35GB at int4. Then add the KV cache, which at long context and high concurrency frequently exceeds the weights. If it does not fit, nothing else matters.<br><br><strong>Memory bandwidth is the second, and it determines decode speed</strong> — decode is bandwidth-bound, so tokens/sec tracks bandwidth almost linearly at small batch. H100 (~3.3TB/s HBM3) versus A100 (~2TB/s) versus L40S (~864GB/s) is largely a decode-throughput ranking. Compare bandwidth, not TFLOPs, for decode-heavy workloads.<br><br><strong>Compute matters for prefill</strong> — long prompts, high prompt-to-output ratios, and batch processing are FLOPs-bound, where H100's tensor cores and fp8 support pull ahead sharply.<br><br><strong>Interconnect matters if the model spans GPUs:</strong> tensor parallelism is communication-heavy per layer, so NVLink versus PCIe is a large difference. Do not shard across PCIe-connected GPUs if you can avoid it.<br><br><strong>The practical decision:</strong> fit the model on the fewest GPUs possible (multi-GPU adds communication overhead and complexity), prefer newer generations for bandwidth and fp8, and evaluate cost per token rather than cost per hour — a more expensive GPU with 3x throughput is cheaper per token.<br><br><strong>Benchmark on your actual workload.</strong> Vendor numbers assume favorable batch sizes and sequence lengths that may not resemble yours.` },

{ q: 'What is model parallelism vs data parallelism?', tags: ['FREQUENTLY ASKED'], a: `<strong>Data parallelism:</strong> replicate the full model on every GPU, split the <em>batch</em> across them, and all-reduce gradients after each backward pass. Simple, scales well, and is the default when the model fits in one GPU's memory. The constraint is exactly that — it does not help if the model itself is too large.<br><br><strong>Model parallelism:</strong> split the <em>model</em> across GPUs. Two main forms:<br>• <strong>Tensor parallelism</strong> — split individual layers' weight matrices across GPUs (attention heads, FFN dimensions), with an all-reduce at each layer boundary. Communication-heavy per forward pass, so it demands fast interconnect (NVLink), which caps it to within-node in practice.<br>• <strong>Pipeline parallelism</strong> — assign contiguous <em>layers</em> to different GPUs and pass activations between them. Much less communication (only at stage boundaries), so it works across nodes — but it introduces the pipeline bubble, idle time while stages wait, mitigated by splitting batches into micro-batches.<br><br><strong>In practice you combine them ("3D parallelism"):</strong> tensor parallelism within a node (fast interconnect), pipeline parallelism across nodes, data parallelism across pipeline replicas. Large-model training uses all three.<br><br><strong>The distinction that matters for inference:</strong> data parallelism is about <em>throughput</em> — more replicas serve more requests. Model parallelism is about <em>feasibility</em> — it exists because the model does not fit, and it costs latency through communication. Use the fewest GPUs the model requires, then replicate.` },

{ q: 'What is tensor parallelism, and how does it help serve large models?', tags: [], a: `Tensor parallelism splits individual weight matrices across GPUs so each holds a slice, computes a partial result, and the results are combined with a collective operation.<br><br><strong>Concretely, in a Transformer:</strong> the FFN's first matrix is split column-wise (each GPU computes part of the hidden dimension) and the second row-wise, so the layer needs only one all-reduce at the end. Attention splits by <em>heads</em> — each GPU owns a subset of heads end to end — which is a natural partition since heads are already independent. Megatron-LM established this arrangement, chosen specifically to minimize the number of synchronization points.<br><br><strong>Why it matters for serving:</strong> it is what makes models larger than one GPU's memory servable at all, and unlike pipeline parallelism it does not add pipeline bubbles — every GPU works on every token. It also aggregates memory <em>bandwidth</em>: 8 GPUs give 8x the bandwidth, and since decode is bandwidth-bound, tensor parallelism can improve per-token latency, not just capacity.<br><br><strong>The cost is communication.</strong> There are two all-reduces per Transformer layer, on every forward pass. On NVLink that is acceptable; over PCIe or across nodes it dominates and destroys the benefit. <strong>Keep tensor parallelism within a node.</strong><br><br><strong>Practical guidance:</strong> the degree must divide the head count evenly. Use the smallest degree that fits the model plus KV cache — more GPUs means more communication overhead and worse efficiency per GPU, so scaling TP beyond necessity is a net loss.` },

{ q: 'What is pipeline parallelism?', tags: [], a: `Pipeline parallelism assigns <em>contiguous groups of layers</em> to different GPUs. Layer 1–8 on GPU 0, 9–16 on GPU 1, and so on. Activations flow forward between stages, gradients flow back.<br><br><strong>Its advantage over tensor parallelism is communication volume:</strong> you transfer only the activation tensor at each stage boundary, not an all-reduce of the full hidden state at every layer. That makes it viable <em>across nodes</em>, where tensor parallelism is not.<br><br><strong>The characteristic problem is the pipeline bubble.</strong> With naive scheduling, GPU 1 idles while GPU 0 processes the first input, and GPU 0 idles at the end. Idle fraction is roughly (stages−1)/(micro-batches + stages−1). <strong>Micro-batching is the fix</strong> — split the batch into many small pieces so stages stay busy — and GPipe, PipeDream, and interleaved 1F1B schedules progressively reduce the bubble further.<br><br><strong>For training this works well.</strong> For <em>inference</em> it is much less attractive: single-request latency is the sum of all stages with no parallelism gain, and you need many concurrent requests in flight to fill the pipeline. So pipeline parallelism improves throughput but not latency.<br><br><strong>Practical rule:</strong> use tensor parallelism within a node (fast NVLink, latency-friendly), pipeline parallelism across nodes when the model exceeds a single node's memory, and data parallelism to replicate. Reach for pipeline parallelism only when tensor parallelism alone cannot fit the model.` },

{ q: 'How does continuous batching improve throughput?', tags: ['CRITICAL'], a: `<strong>Static batching wastes most of the GPU.</strong> Collect N requests, run them together, wait for <em>all</em> to finish. Since generation lengths vary widely — one request finishes in 20 tokens, another needs 500 — the short sequences sit padded and idle while the longest one completes. GPU utilization collapses.<br><br><strong>Continuous (in-flight) batching operates at the <em>iteration</em> level instead:</strong> at every decode step, evict sequences that have finished and admit waiting requests into the freed slots. The batch composition changes constantly, and the GPU never waits on a straggler.<br><br><strong>The gain is large</strong> — commonly several-fold to an order of magnitude in throughput versus static batching under realistic variable-length traffic, and it is the primary reason a purpose-built inference server beats a naive generation loop.<br><br><strong>What makes it work:</strong> the KV cache must support adding and removing sequences without reallocating contiguous blocks — which is exactly what PagedAttention provides. The two techniques are complementary and usually deployed together; continuous batching without paged memory hits fragmentation limits quickly.<br><br><strong>The scheduling subtlety:</strong> admitting a new request means running its <em>prefill</em>, which is compute-heavy and stalls the decode steps of everyone else — visible as an inter-token latency spike. Chunked prefill splits long prompts into pieces interleaved with decode steps to smooth this out.<br><br><strong>The tradeoff to state:</strong> larger batches raise throughput and lower cost per token, but queue delay raises TTFT. Serving economics is choosing that operating point, and it should differ per tier.` },

{ q: 'What is speculative decoding?', tags: ['CRITICAL'], a: `Speculative decoding uses a small fast <strong>draft</strong> model to propose k tokens, then the large <strong>target</strong> model verifies all k in a <em>single</em> forward pass, accepting the longest correct prefix.<br><br><strong>Why it works:</strong> decode is memory-bandwidth-bound, not compute-bound. Verifying 5 tokens costs almost the same wall-clock time as generating 1, because you read the model weights once either way — the arithmetic is nearly free. So you convert a latency problem into a parallel-verification problem.<br><br><strong>The critical property:</strong> with the correct rejection-sampling acceptance criterion, the output distribution is <strong>mathematically identical</strong> to sampling from the target model alone. This is not an approximation — it is a pure latency optimization with no quality cost, which is what makes it broadly deployable.<br><br><strong>Typical gains:</strong> 2–3x on the latency-critical path, depending on <em>acceptance rate</em> — how often the draft agrees with the target. Acceptance depends on draft quality and on the domain (predictable text like code and structured output has high acceptance; creative generation is lower).<br><br><strong>Variants worth knowing:</strong> <em>self-speculation</em> (Medusa adds extra prediction heads to the same model, avoiding a separate draft model), <em>n-gram/prompt lookup</em> (draft by copying from the prompt — extremely effective for summarization and code editing where output repeats input), and EAGLE-style feature-level drafting.<br><br><strong>The tradeoff:</strong> it improves per-request <em>latency</em> but consumes extra compute per token, so under heavy batching — where you are already throughput-bound — it can reduce total throughput. Use it for latency-sensitive, low-concurrency paths.` },

{ q: 'What is the KV cache, and how do you manage its memory?', tags: ['CRITICAL'], a: `During decoding each new token attends over all previous tokens. Causal masking means past tokens' key and value vectors never change, so you cache them instead of recomputing — turning O(n²) work per step into O(n).<br><br><strong>Size it explicitly:</strong> <code>2 × layers × kv_heads × head_dim × seq_len × batch × dtype_bytes</code>. At long context and high concurrency this frequently exceeds the model weights, and <strong>it is usually what caps concurrency, not compute</strong>. Max concurrent sequences ≈ (VRAM − weights) ÷ per-sequence cache.<br><br><strong>Management techniques, by leverage:</strong><br>• <strong>PagedAttention</strong> — store the cache in fixed-size non-contiguous blocks like OS virtual memory. Eliminates the internal fragmentation that otherwise wastes 60–80% of allocated cache, and enables copy-on-write prefix sharing across requests. The single biggest practical win.<br>• <strong>GQA/MQA</strong> — architectural; sharing K/V heads across query heads cuts the cache 4–8x. Must be chosen at pretraining.<br>• <strong>Quantize the cache</strong> to fp8/int8 for 2–4x, usually cheaper in quality terms than quantizing weights.<br>• <strong>Eviction</strong> — sliding window, or StreamingLLM (keep the first few "attention sink" tokens plus a recent window, which preserves fluency far better than a naive window).<br>• <strong>Offload</strong> to CPU memory for idle sequences.<br><br><strong>The caution on eviction:</strong> it is silent information loss. The model does not report that it dropped the middle of the document — it just answers worse. Validate any eviction policy against a long-context retrieval eval, not perplexity.` },

{ q: 'What is PagedAttention?', tags: ['CRITICAL'], a: `PagedAttention applies operating-system virtual memory concepts to the KV cache. Instead of allocating one contiguous buffer per sequence, it stores the cache in <strong>fixed-size blocks</strong> that need not be contiguous, with a per-sequence block table mapping logical token positions to physical blocks.<br><br><strong>The problem it solves is fragmentation.</strong> Naive serving pre-allocates a contiguous buffer sized for the <em>maximum</em> possible sequence length, because you cannot know in advance how long generation will run. Since most sequences are far shorter, 60–80% of allocated cache is wasted — and since KV cache memory caps concurrency, that waste directly caps throughput. PagedAttention reduces waste to under ~4% (only the last partial block per sequence).<br><br><strong>The second benefit is sharing.</strong> Blocks are reference-counted with copy-on-write, so multiple requests sharing a prefix — a common system prompt, or parallel samples from the same prompt — physically share those blocks instead of duplicating them. This is the mechanism underlying prompt caching and efficient beam search.<br><br><strong>Why it matters:</strong> more concurrent sequences in the same memory means larger effective batches, and larger batches mean better GPU utilization. Combined with continuous batching, it is the main reason vLLM delivers roughly an order of magnitude more throughput than naive HuggingFace generation.<br><br><strong>The cost:</strong> a custom attention kernel that gathers from non-contiguous blocks, plus block-table bookkeeping — a small constant overhead, vastly outweighed by the capacity gain. It is now standard across serving frameworks.` },

{ q: 'How does GGUF work?', tags: [], a: `GGUF (GPT-Generated Unified Format) is the model file format used by llama.cpp, designed for efficient local and CPU-capable inference. It replaced the earlier GGML format.<br><br><strong>What it provides:</strong> a single self-contained file holding weights, tokenizer, and metadata (architecture, hyperparameters, chat template) — so there are no external config dependencies. It is designed for <strong>memory-mapping</strong>, meaning the OS pages weights in on demand rather than loading everything into RAM up front. That gives near-instant startup and lets you run models larger than available RAM, backed by disk.<br><br><strong>Quantization is its main feature.</strong> GGUF supports a range of "k-quant" schemes — Q4_K_M, Q5_K_M, Q8_0 and others — where the letter suffix indicates the mixing strategy. These use <em>mixed precision within the model</em>: more sensitive tensors (attention, and often the output layer) are kept at higher precision while the bulk is quantized harder. Q4_K_M is the common quality/size sweet spot.<br><br><strong>Also:</strong> flexible CPU/GPU layer offloading — you specify how many layers live on GPU and the rest run on CPU, which is what makes partial acceleration on consumer hardware practical.<br><br><strong>Where it fits:</strong> local inference, desktop apps, edge deployment, and anything CPU-bound or memory-constrained. It powers Ollama, LM Studio, and llama.cpp.<br><br><strong>Where it does not:</strong> high-throughput server serving. For that, vLLM or TensorRT-LLM with GPU-native formats (GPTQ, AWQ, fp8) is substantially faster — GGUF optimizes for a different constraint.` },

{ q: 'How do you optimize inference for edge and mobile?', tags: ['TRADEOFF'], a: `<strong>The constraints drive everything:</strong> a few GB of RAM, no sustained high power draw (thermal throttling and battery), limited memory bandwidth, and no guaranteed network.<br><br><strong>Model selection first:</strong> a 1–4B parameter SLM, quantized to 4-bit — roughly 0.5–2GB. Prefer models with GQA, since memory bandwidth is the binding constraint on tokens/sec on-device. Consider distilling a task-specific model rather than running a general one; a fine-tuned 1B model on a narrow task often beats a general 7B and fits comfortably.<br><br><strong>Quantization is mandatory, not optional</strong> — int4 for weights, and check whether the runtime supports quantized KV cache too. Larger models tolerate quantization better, so there is a real floor: a 1B model at 4-bit may degrade unacceptably where a 3B does not.<br><br><strong>Runtime matters as much as the model:</strong> Core ML / MLX on Apple, ONNX Runtime, TensorFlow Lite, llama.cpp, MediaPipe. Use the platform's NPU where available — it is far more power-efficient than GPU or CPU for this workload, which matters more than raw speed on battery.<br><br><strong>Engineering details:</strong> memory-map weights for fast startup, lazy-load and unload under memory pressure, cap generation length to protect battery, and degrade gracefully on older hardware — device fragmentation is a real product constraint.<br><br><strong>The architecture that actually works is hybrid:</strong> an on-device classifier routes per request, handling the high-frequency simple cases locally and escalating hard queries to cloud. That gives privacy and offline capability on the common path with frontier quality on the tail.` },

{ q: 'What is quantization (INT8, INT4, FP16, BF16), and how does it affect quality?', tags: ['CRITICAL'], a: `<strong>The formats:</strong><br>• <em>FP32</em> — 4 bytes, training baseline, rarely used for inference.<br>• <em>FP16</em> — 2 bytes, 10-bit mantissa, narrow exponent range so it can overflow/underflow during training.<br>• <em>BF16</em> — 2 bytes, same exponent range as FP32 with less mantissa precision. More numerically robust for training, which is why it became the default.<br>• <em>FP8</em> — 1 byte, hardware-native on H100+, near-lossless in practice.<br>• <em>INT8</em> — 1 byte, essentially lossless for inference with a calibrated method.<br>• <em>INT4</em> — 0.5 bytes, usually acceptable with AWQ or GPTQ, degrades noticeably below.<br><br><strong>Why it helps more than the memory math suggests:</strong> decode is memory-bandwidth-bound, so halving bytes read per token nearly doubles decode speed. Quantization is a latency win, not just a capacity win.<br><br><strong>Quality effects, honestly:</strong> 8-bit is safe. 4-bit with a good method (AWQ protects salient channels; GPTQ minimizes layer output error) is usually acceptable. Below 4-bit degrades sharply. <strong>Larger models tolerate quantization better than small ones</strong> — a 70B at 4-bit often beats a 13B at 8-bit for the same memory.<br><br><strong>The measurement trap to name:</strong> perplexity barely moves while task performance can drop meaningfully, especially on reasoning, math, and long-context tasks. Always evaluate on your actual task, and check whether degradation concentrates in a specific category rather than spreading evenly.<br><br><strong>Also quantize the KV cache</strong> — often a bigger practical win at long context than quantizing weights further.` },

{ q: 'How do you implement auto-scaling for AI workloads?', tags: ['TRADEOFF'], a: `<strong>Lead with the constraint that breaks conventional autoscaling: GPU cold starts are minutes.</strong> You must provision the instance, pull a multi-gigabyte container, and load tens of GB of weights into VRAM. Reactive scaling on CPU-style signals arrives long after the spike has passed.<br><br><strong>So scale on leading indicators, not lagging ones:</strong> queue depth and time-in-queue are the right signals — they rise before latency degrades. GPU utilization is a poor scaling metric because a saturated GPU with a short queue is healthy, and an idle GPU with a long queue means something else is wrong.<br><br><strong>Combine three strategies:</strong><br>• <em>Predictive/scheduled</em> — most production traffic is diurnal and forecastable. Pre-scale before the known morning ramp rather than chasing it.<br>• <em>Warm pools</em> — keep instances provisioned and loaded but idle, so scale-out is seconds. Expensive but the only way to absorb genuine spikes.<br>• <em>Reactive</em> — as a slow backstop for sustained shifts.<br><br><strong>Set asymmetric thresholds:</strong> scale up aggressively, scale down slowly with a long cooldown. Thrashing is expensive when each cycle costs minutes of loading.<br><br><strong>Admission control is the necessary complement.</strong> Autoscaling cannot respond within the spike, so you must degrade gracefully at the ceiling: bounded queues, priority tiers, honest 429s with <code>Retry-After</code>, and load shedding of expensive optional features.<br><br><strong>For API-based systems</strong> this becomes quota management and multi-provider distribution rather than instance scaling — same principle, different lever.` },

{ q: 'What is the role of load balancing in AI serving?', tags: [], a: `<strong>Conventional load balancing algorithms work badly for LLM serving,</strong> and understanding why is the substance of this question.<br><br>Round-robin and least-connections assume requests are roughly equal cost. For LLMs they are not — one request may be 100 input tokens and 20 output tokens, another 50,000 input and 2,000 output. Balancing by connection count routes a huge prefill onto a node already saturated with long generations.<br><br><strong>Better signals:</strong> route on <em>KV cache utilization</em> or number of active tokens rather than request count — that reflects actual load. Many serving stacks expose these metrics for exactly this purpose.<br><br><strong>Prefix-aware routing is the distinctive LLM technique:</strong> route requests sharing a prompt prefix (the same system prompt, the same document, the same conversation) to the <em>same</em> replica so the cached KV state is reused. Ignoring this means every replica recomputes the same prefill, wasting a large fraction of your capacity. This can matter more than balance itself.<br><br><strong>Session affinity</strong> for multi-turn conversations, so the growing prefix stays cache-warm on one node.<br><br><strong>Other responsibilities:</strong> health checking that verifies the model actually responds correctly, not just that the port is open — a node with a corrupted or unloaded model returns 200s and garbage. Routing by model type when serving several. Priority-aware distribution so interactive traffic is not queued behind batch.<br><br><strong>The design point:</strong> a good LLM load balancer optimizes for cache locality and token-weighted load, not for even request counts.` },

{ q: 'How do you manage GPU memory for serving multiple models?', tags: ['TRADEOFF'], a: `<strong>Enumerate what consumes VRAM:</strong> model weights, KV cache (frequently the largest term at scale), activations, and framework/CUDA overhead. Then choose a strategy.<br><br><strong>Dedicated GPUs per model</strong> — simplest, full isolation, no interference. Wasteful when models are idle, which for a long tail of low-traffic models is most of the time.<br><br><strong>Co-locating multiple models</strong> on one GPU with explicit memory fractions. Works when models are small and traffic is steady; the risk is that one model's KV cache growth starves another, so cap each explicitly rather than letting them compete.<br><br><strong>Multi-LoRA serving — the best answer when applicable.</strong> One base model in memory with many LoRA adapters (megabytes each) hot-swapped per request. This is what makes hundreds of fine-tuned variants economically viable; the alternative of one full model per variant does not scale at all.<br><br><strong>Dynamic loading with an LRU cache</strong> for a long tail of models. The problem is load time — tens of GB from disk is tens of seconds — so this only works for latency-tolerant traffic, and you should pin the hot models.<br><br><strong>The KV cache is the tuning knob.</strong> Most serving frameworks let you set the fraction of VRAM reserved for it. Too small and concurrency collapses; too large and you cannot fit another model. Size it from your actual concurrency and context-length distribution rather than defaults.<br><br><strong>Practical guidance:</strong> quantize to free headroom, and monitor peak usage — OOM under load is the failure mode, and it is abrupt.` },

{ q: 'What is model sharding, and when would you use it?', tags: [], a: `Model sharding splits a model's parameters across multiple devices because it does not fit on one. It is the same concept as model parallelism, viewed from the memory side.<br><br><strong>Forms:</strong> <em>tensor sharding</em> (split individual weight matrices — tensor parallelism), <em>layer sharding</em> (assign layer ranges to devices — pipeline parallelism), and <em>state sharding</em> for training (FSDP / ZeRO, which shard parameters, gradients, and optimizer states across data-parallel workers and gather them just in time).<br><br><strong>When you need it:</strong> the arithmetic decides. Weights are ~2 bytes/parameter at fp16, so a 70B model is ~140GB — more than any single current GPU. Add the KV cache and activations. If the total exceeds one device, you shard.<br><br><strong>When you should avoid it:</strong> whenever quantization can make the model fit on fewer devices. Sharding introduces communication overhead on every forward pass, adds failure modes, and complicates deployment. A 70B at int4 (~35GB) fits on one 80GB GPU with room for cache — strictly better than sharding it at fp16 across two, for most workloads.<br><br><strong>Practical rules:</strong> use the smallest shard count that fits; keep tensor sharding within a node where NVLink is available, since it is communication-heavy per layer; and use pipeline sharding across nodes where communication is limited to stage boundaries.<br><br><strong>The decision order:</strong> quantize first, shard second. Sharding is a capability enabler, not an optimization — it costs latency and complexity, and you take it only when you must.` },

{ q: 'How do you implement request queuing and priority scheduling?', tags: [], a: `<strong>Bounded queues are the first requirement.</strong> Most "the spike took us down" incidents are unbounded queueing: the system accepts everything, memory fills or every request times out, and you fail 100% instead of serving 70%. Cap the queue and reject cleanly above it with an honest <code>Retry-After</code>.<br><br><strong>Priority tiers, defined by what your product actually needs:</strong> interactive before batch, paid before free, short before long. Implement with multiple queues and weighted selection rather than strict priority — strict priority starves low tiers indefinitely under sustained load, which is usually not what you want.<br><br><strong>The LLM-specific scheduling decisions:</strong><br>• <em>Estimate cost at admission,</em> not just count requests. A 50k-token prompt and a 200-token prompt are not equivalent, so admission control should be token-aware.<br>• <em>Separate or interleave prefill and decode.</em> A long prefill stalls in-flight decodes and spikes inter-token latency for everyone. Chunked prefill smooths this; disaggregated serving eliminates it.<br>• <em>Prefix-aware routing</em> — group requests sharing a prompt prefix onto the same replica for cache reuse. This can matter more than balance.<br><br><strong>Fairness:</strong> per-tenant concurrency caps so one customer cannot occupy every slot while staying under an RPM limit. Concurrency, not request rate, is what maps to hardware.<br><br><strong>Observability:</strong> track queue depth, time-in-queue, and rejection rate per tier. Time-in-queue is the leading indicator — it rises before latency SLOs break, giving you room to scale or shed.` },

{ q: 'What are the cost tradeoffs between self-hosted and API inference?', tags: ['TRADEOFF'], a: `<strong>API:</strong> pay per token, zero fixed cost, instant scaling, no operational burden, immediate access to frontier models. Cost scales linearly with usage — which is ideal at low volume and punishing at high volume.<br><br><strong>Self-hosted:</strong> pay for GPU-hours whether used or not. An H100 is roughly $2–4/hour on demand, less reserved. The economics invert at high, <em>steady</em> utilization — but only if you actually keep the GPUs busy.<br><br><strong>The break-even calculation:</strong> compute your cost per million tokens self-hosted (GPU hourly cost ÷ achievable tokens/hour) and compare to API pricing. The critical variable is <strong>utilization</strong>: a GPU at 20% utilization has 5x the effective cost per token. Bursty traffic destroys self-hosting economics, because you provision for peak and pay for it at trough.<br><br><strong>Costs people omit from the self-hosted side:</strong> engineering time (a serving stack is not free to build or operate), on-call burden, over-provisioning for peak, idle time, and the model quality gap — an open model may need more tokens or more retries to reach the same result.<br><br><strong>Where self-hosting wins on non-cost grounds:</strong> data residency and privacy, version pinning (no silent model changes), latency guarantees, and freedom to fine-tune, quantize, and distill.<br><br><strong>My recommendation:</strong> start on an API. Move to self-hosting when you have sustained high volume with predictable traffic, or a hard compliance requirement. And the highest-leverage middle path is often a <em>fine-tuned small self-hosted model</em> for the high-volume easy majority, with API escalation for the hard tail.` },

{ q: 'How do you handle cold start latency for serverless AI?', tags: ['TRADEOFF'], a: `<strong>Understand what dominates:</strong> for GPU workloads, cold start is container pull (multi-GB images) plus weight loading into VRAM (tens of GB) plus CUDA/framework initialization. Total is typically 30 seconds to several minutes — an order of magnitude worse than CPU serverless, which is why "just use serverless" often fails for LLMs.<br><br><strong>Reduce load time:</strong><br>• <em>Shrink the image</em> — do not bake weights into the container; pull them separately from fast storage.<br>• <em>Stream weights</em> rather than downloading fully before starting (approaches like tensorizer or safetensors with memory-mapping let inference begin before the full load completes).<br>• <em>Quantize</em> — a 4-bit model loads 4x faster than fp16 purely because there are fewer bytes.<br>• <em>Cache weights on local NVMe</em> so subsequent starts skip the network entirely.<br><br><strong>Avoid the cold start:</strong> keep-warm instances (the standard answer — provisioned concurrency, at a cost), scheduled pre-warming ahead of known traffic patterns, and periodic health pings to prevent scale-to-zero.<br><br><strong>Design around it:</strong> route the first request to a warm fallback (a smaller always-on model or an API) while capacity spins up, and make the wait visible if it is unavoidable.<br><br><strong>The honest conclusion:</strong> for latency-sensitive LLM serving, true scale-to-zero serverless is usually the wrong architecture. It fits batch and asynchronous workloads well. For interactive traffic, a small always-on baseline plus autoscaling above it is more practical — you pay for the floor and get predictable latency.` },

{ q: 'How do you implement model caching to reduce redundant computation?', tags: [], a: `<strong>Four distinct caching layers, with different hit rates and risk profiles.</strong><br><br><strong>1. Prefix/KV caching (highest value).</strong> Reuse the computed KV cache for a shared prompt prefix, skipping prefill for those tokens entirely. Up to ~90% input cost reduction plus a large TTFT drop. <em>This dictates prompt ordering</em> — static content (system prompt, tool definitions, examples) first, variable content last. Reordering for cache-friendliness is often the cheapest large win available and costs no quality.<br><br><strong>2. Exact-match response cache.</strong> Hash (prompt + model + params) → response. Zero risk, low hit rate on natural language since users rarely phrase identically. Still worth having for programmatic and repeated queries.<br><br><strong>3. Semantic cache.</strong> Embed the query, return a cached response above a similarity threshold. Much higher hit rate, and <em>genuinely risky</em> — a false hit returns a wrong answer with no error. "Cancel my order" and "don't cancel my order" have near-identical embeddings and opposite meanings. Use a conservative threshold, restrict to FAQ-like traffic, and never apply where small wording differences change the answer.<br><br><strong>4. Component caches</strong> — query embeddings, retrieval results, tool outputs within a run.<br><br><strong>Correctness requirements:</strong> key by tenant and permission scope, or one user's cached answer leaks to another. Invalidate on knowledge-base updates. Never cache personalized or time-sensitive responses.<br><br><strong>Measure hit rate <em>and</em> hit quality</strong> — sample semantic hits and verify appropriateness. An unmeasured semantic cache is a silent quality regression.` },

{ q: 'What is the difference between synchronous and asynchronous inference?', tags: [], a: `<strong>Synchronous:</strong> the client holds a connection and waits for the result. Simple, low-latency for short work, and the natural fit for interactive use — chat, autocomplete, search.<br><br><strong>Asynchronous:</strong> the client submits a job, receives an id immediately, and retrieves the result later via polling, webhook, or a push channel. Necessary when the work takes longer than a reasonable HTTP timeout.<br><br><strong>The decision boundary:</strong> if the work reliably completes in under ~30 seconds and a user is waiting, synchronous with streaming. If it takes minutes, is bursty, or nobody is waiting, asynchronous.<br><br><strong>Where async is mandatory:</strong> image and video generation, long agent trajectories, batch document processing, and any embedding or ingest pipeline. Trying to serve these synchronously produces timeouts, retry storms, and connection exhaustion — and the retries multiply load exactly when you are already saturated.<br><br><strong>What async buys beyond avoiding timeouts:</strong> load smoothing (queue absorbs bursts rather than rejecting them), cheaper compute (batch API tiers are typically ~50% off), better GPU utilization through large-batch scheduling, and durability — a job survives a client disconnect or a worker crash if you checkpoint.<br><br><strong>Streaming is the important middle ground</strong> and often the right answer for interactive work: synchronous in structure, but the user sees tokens immediately, so a 20-second generation feels responsive. Do not jump to async when streaming would solve the perceived-latency problem.<br><br><strong>For async, design the client contract carefully:</strong> job status, progress, partial results, cancellation, and idempotency keys so a retry does not duplicate the work.` },

{ q: 'What is FSDP, and how does it differ from DeepSpeed ZeRO?', tags: [], a: `Both solve the same problem: in standard data parallelism every GPU holds a full copy of parameters, gradients, and optimizer states — and with Adam, optimizer states are roughly 2x the parameters in fp32, so memory, not compute, becomes the constraint on model size.<br><br><strong>The shared idea:</strong> shard those states across data-parallel workers and gather them just-in-time. <strong>ZeRO stages:</strong> Stage 1 shards optimizer states (~4x saving), Stage 2 adds gradients (~8x), Stage 3 adds parameters themselves — each GPU holds only a slice and all-gathers a layer's parameters right before using it, then discards them. ZeRO-Offload and ZeRO-Infinity extend this to CPU memory and NVMe.<br><br><strong>FSDP</strong> (PyTorch's Fully Sharded Data Parallel) is essentially ZeRO Stage 3 implemented natively in PyTorch. Functionally very similar; the differences are practical:<br>• <em>FSDP</em> — native PyTorch, better integration with the ecosystem (torch.compile, PyTorch-native checkpointing), and the direction of upstream investment. FSDP2 improves the sharding representation and composability.<br>• <em>DeepSpeed</em> — a separate library with a broader feature set: the full ZeRO stage ladder, aggressive CPU/NVMe offload, and mature pipeline/tensor parallelism integration.<br><br><strong>The tradeoff either way:</strong> you trade communication for memory. Parameters are gathered and released repeatedly, so interconnect bandwidth matters enormously — on slow networking, sharding can be net-negative.<br><br><strong>Practical recommendation:</strong> FSDP if you are in the PyTorch ecosystem; DeepSpeed when you need aggressive offload or its specific parallelism combinations.` },

{ q: 'How do you monitor and profile LLM inference in production?', tags: ['CRITICAL'], a: `<strong>Track the two latency metrics separately</strong> — this is the LLM-specific part. <em>TTFT</em> reflects prefill and queue time; <em>inter-token latency</em> (or tokens/sec) reflects decode. A single "latency" number hides which half is broken and leads to optimizing the wrong thing.<br><br><strong>Throughput and utilization:</strong> requests/sec, tokens/sec, and — the metric that actually matters — <strong>goodput</strong>, requests completed within SLO. High raw throughput while missing latency targets is not delivering.<br><br><strong>Queue metrics:</strong> depth, time-in-queue, and rejection rate. Time-in-queue is the best leading indicator — it rises before latency SLOs break, giving you room to scale or shed.<br><br><strong>GPU metrics:</strong> utilization, memory used, and <em>KV cache utilization</em> specifically. Cache utilization is the concurrency ceiling, so it predicts when you will start rejecting; GPU utilization alone is misleading because a bandwidth-bound decode phase can show high utilization while doing little useful work.<br><br><strong>Batch metrics:</strong> average batch size and batch composition. A consistently small batch under load means the scheduler or memory configuration is limiting you, and it is a common source of unexplained low throughput.<br><br><strong>Profiling when you need depth:</strong> Nsight Systems or PyTorch Profiler to find kernel-level bottlenecks, and the serving framework's own metrics (vLLM exposes detailed scheduler and cache statistics) — usually more actionable than kernel traces.<br><br><strong>Correlate with cost and quality:</strong> tokens per request, cost per request as a distribution (the tail drives the bill), and schema-failure rate as an early quality signal.` },

{ q: 'What is infrastructure-level model routing?', tags: ['CRITICAL'], a: `Model routing directs each request to an appropriate model based on estimated difficulty, cost, latency requirements, or capability needs — rather than sending everything to one model.<br><br><strong>Why it is the largest cost lever after prompt caching:</strong> most production traffic is easy. Serving it all with a frontier model over-provisions the common case massively. Routing lets you spend proportionally to difficulty, commonly cutting cost several-fold while preserving quality on the hard tail.<br><br><strong>Routing signals:</strong> query complexity (classifier or embedding-based), required capability (does it need tools, vision, long context, or reasoning), latency tier, cost budget, and tenant plan.<br><br><strong>Implementation options, cheapest first:</strong> <em>embedding similarity</em> against labeled example queries per route (sub-10ms, essentially free); <em>a fine-tuned small classifier</em> (fast, accurate, the right answer with labeled data); <em>an LLM router</em> (flexible, but adds a full model call to every request, which partly defeats the purpose). Frameworks like RouteLLM formalize the cost/quality tradeoff explicitly.<br><br><strong>The pattern that avoids router mistakes: cascading.</strong> Try the small model, and escalate to the large one when a confidence check or verifier fails. This is more robust than upfront classification because the escalation decision is made <em>after</em> seeing the small model's actual answer — you pay a little extra on the escalated fraction and eliminate misrouting.<br><br><strong>Measure per-route quality, not aggregate.</strong> A misrouted hard query produces a confidently poor answer with no signal, and aggregate metrics hide it. Bias thresholds toward escalation.` },
]};
