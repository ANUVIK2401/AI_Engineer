/* Multimodal AI — 26 questions */
window.QA_SECTION = {
  id: '11',
  slug: 'multimodal',
  title: 'Multimodal AI',
  blurb: 'Vision-language models, CLIP, diffusion, speech, document understanding, and cross-modal failure modes.',
  questions: [
{ q: 'What are multimodal AI models, and how do they process different data types?', tags: ['CRITICAL'], a: `A multimodal model processes and relates information across two or more modalities — text, images, audio, video. The unifying mechanism is that <strong>every modality is converted into a sequence of vectors in a shared representation space</strong>, after which a Transformer treats them identically. Attention does not care whether a vector came from a word or an image patch.<br><br><strong>The standard architecture for modern multimodal LLMs</strong> (LLaVA-style): a pretrained vision encoder produces patch embeddings, a projection layer (an MLP, or a cross-attention resampler like Flamingo's Perceiver) maps them into the language model's embedding space, and they are interleaved with text token embeddings as input to the LLM. The language model needs no architectural change — that is precisely why this approach won.<br><br><strong>Training is typically staged:</strong> align the projection layer first with the encoder and LLM frozen, then instruction-tune on multimodal data. This is far cheaper than training end-to-end and reuses two strong pretrained components.<br><br><strong>The key practical consequence:</strong> images consume tokens. A high-resolution image can cost thousands of tokens, and in an agent loop it is re-paid at <em>every subsequent step</em> if it stays in context. That drives both cost and the lost-in-the-middle problem, so extract what you need and keep the raw asset externally.<br><br><strong>The asymmetry worth naming:</strong> most "multimodal" models are multimodal on <em>input</em> only — they see images and emit text. Genuine any-to-any generation is a much harder and less mature capability.` },

{ q: 'How do vision-language models process images?', tags: [], a: `<strong>The pipeline:</strong> the image is resized to a fixed resolution and split into fixed patches (commonly 14×14 or 16×16 pixels). Each patch is linearly projected into a vector — this is the visual analogue of tokenization. Positional embeddings are added, and a Vision Transformer encoder produces contextualized patch representations. A projection layer then maps those into the language model's embedding space, where they are inserted alongside text token embeddings.<br><br><strong>Patch count is the cost driver, and it grows quadratically with resolution.</strong> A 336×336 image at 14×14 patches is 576 tokens; doubling resolution quadruples that. This is the central engineering tension in VLMs: fine detail — small text, dense charts, medical imagery — needs resolution, and resolution is expensive.<br><br><strong>How production systems resolve it:</strong> <em>dynamic tiling</em> (split a high-resolution image into tiles, encode each, plus a downscaled overview — used by LLaVA-NeXT and similar), and <em>resamplers</em> (Q-Former, Perceiver) that compress many patch embeddings into a small fixed set of query vectors, trading some detail for a large token reduction.<br><br><strong>The failure modes this explains:</strong> VLMs are weak at reading small text, counting objects, and precise spatial reasoning — all of which need detail that fixed-resolution patching discards. If your task involves dense document text, the answer is higher-resolution tiling or a dedicated OCR path, not prompt engineering.` },

{ q: 'How does CLIP work, and why is it important?', tags: ['CRITICAL'], a: `CLIP trains two encoders — a vision transformer and a text transformer — with a <strong>contrastive</strong> objective on hundreds of millions of image-caption pairs. Within each batch, the loss pulls matching image-text pairs together and pushes all mismatched pairs apart, using a symmetric cross-entropy over the similarity matrix. The encoders stay separate; the alignment lives entirely in the shared output space.<br><br><strong>Why it mattered:</strong> it demonstrated that <em>natural language is a scalable supervision signal for vision</em>. Instead of hand-labeling images into a fixed taxonomy, you scrape captions from the web — which gives you effectively unlimited, open-vocabulary training data. That unlocked <strong>zero-shot classification</strong>: embed candidate class names as text, take the nearest, and classify without any task-specific training.<br><br><strong>What it enabled downstream:</strong> CLIP's image encoder became the standard vision backbone for multimodal LLMs (LLaVA and successors), and its text encoder conditions Stable Diffusion. Its influence is architectural as much as functional.<br><br><strong>The limitations to state:</strong> trained on short captions, so it captures coarse semantic correspondence rather than fine detail, compositional relations ("a red cube <em>on top of</em> a blue sphere"), counting, or spatial reasoning. It is also much weaker than dedicated text embedders for <em>document</em> retrieval — which is why text-bridged multimodal RAG usually beats native CLIP retrieval. And it inherits web-scale dataset biases directly.` },

{ q: 'What are the key architectures for multimodal models?', tags: [], a: `<strong>Dual-encoder (CLIP-style):</strong> separate encoders per modality trained contrastively into a shared space. Efficient — embeddings precompute, so retrieval is a nearest-neighbor search. But there is no cross-modal interaction beyond a dot product, so it cannot do fine-grained reasoning. Right for retrieval and zero-shot classification.<br><br><strong>Fusion encoder (cross-attention):</strong> modalities interact through cross-attention layers — Flamingo interleaves gated cross-attention blocks into a frozen LLM; BLIP-2's Q-Former uses learned queries to extract visual features into the language space. Better reasoning than dual encoders, at the cost of not being precomputable.<br><br><strong>Projection into an LLM (LLaVA-style) — the dominant pattern:</strong> a vision encoder plus a simple MLP projection mapping patch embeddings into the LLM's token space, with visual tokens simply prepended to the text sequence. It won because it is architecturally trivial, reuses two strong pretrained models, trains cheaply in stages, and inherits the LLM's full reasoning ability.<br><br><strong>Unified/any-to-any:</strong> a single model handling multiple modalities in and out, typically by tokenizing everything into a shared discrete vocabulary (image tokens via a VQ codec). Most general, hardest to train, and least mature.<br><br><strong>The tradeoff axis to name:</strong> efficiency versus interaction depth. Dual encoders are fast and shallow; fusion and projection architectures are expensive and deep. Retrieval wants the first; reasoning wants the second — which is exactly why production multimodal RAG uses a dual encoder to retrieve and a VLM to reason.` },

{ q: 'How does image generation work with diffusion models?', tags: ['CRITICAL'], a: `Diffusion models learn to reverse a noising process. <strong>Forward:</strong> progressively add Gaussian noise to a training image until it is pure noise — a fixed, non-learned process. <strong>Reverse:</strong> train a network (a U-Net, or increasingly a Diffusion Transformer) to predict the noise added at each step. Generation starts from random noise and iteratively denoises over 20–50 steps.<br><br><strong>The key efficiency innovation is <em>latent</em> diffusion</strong> (Stable Diffusion): run the whole process in a compressed latent space from a VAE — typically 8x smaller per side, so ~64x fewer elements — rather than in pixel space. That is what made high-resolution generation affordable on consumer hardware.<br><br><strong>Text conditioning</strong> comes via cross-attention: text embeddings (from CLIP's text encoder, or T5) are attended to at each denoising step, so the prompt steers every step rather than only the start. <strong>Classifier-free guidance</strong> then amplifies the conditioning — run the model both with and without the prompt and extrapolate away from the unconditional prediction. The guidance scale is the prompt-adherence/diversity dial, and pushing it high produces over-saturated, low-diversity images.<br><br><strong>Why diffusion beat GANs:</strong> stable training (a simple regression loss, no adversarial minimax), better mode coverage and diversity, and natural conditioning. The cost is that generation requires many sequential forward passes rather than one — which is what distillation methods (LCM, Turbo, adversarial distillation) attack, reaching 1–4 steps.` },

{ q: 'What is text-to-speech, and what models are used?', tags: [], a: `TTS synthesizes speech audio from text. Modern systems are <strong>neural end-to-end</strong>, having replaced the older concatenative and parametric approaches.<br><br><strong>The classic two-stage pipeline:</strong> an acoustic model maps text (or phonemes) to a mel-spectrogram — Tacotron 2 (autoregressive), FastSpeech 2 (non-autoregressive, much faster and more robust) — then a vocoder converts the spectrogram to a waveform (HiFi-GAN, WaveGlow). VITS unified both stages end-to-end with a VAE plus adversarial training.<br><br><strong>The current generation is LLM-shaped:</strong> encode audio into discrete tokens with a neural codec (EnCodec, SoundStream), then model the token sequence autoregressively conditioned on text and a short reference clip. VALL-E, Bark, and XTTS work this way. The breakthrough capability is <strong>zero-shot voice cloning</strong> — matching a speaker's timbre and prosody from a few seconds of reference audio, with no per-speaker training.<br><br><strong>Production considerations:</strong> streaming synthesis (generate and emit in chunks) is essential for conversational latency — you need first-audio in a few hundred milliseconds. Text normalization is an underrated source of errors: numbers, dates, abbreviations, and units must be expanded correctly before synthesis, and this is where most quality complaints actually originate.<br><br><strong>The ethical constraint that shapes design:</strong> voice cloning enables impersonation and fraud. Consent verification, watermarking, and refusing to clone without authorization are requirements, not optional policy.` },

{ q: 'How does Whisper work?', tags: [], a: `Whisper is an <strong>encoder-decoder Transformer</strong> trained on 680,000 hours of weakly-supervised multilingual audio scraped from the web. Audio is converted to an 80-channel log-Mel spectrogram over 30-second windows, the encoder processes it, and the decoder autoregressively produces text tokens.<br><br><strong>The contribution was the training approach, not the architecture.</strong> Prior ASR relied on carefully curated, human-transcribed corpora and used self-supervised pretraining plus fine-tuning. Whisper showed that massive <em>weakly supervised</em> data — noisy, varied, real-world audio-text pairs — produces a model that is far more robust to accents, background noise, and domain shift, and works zero-shot without dataset-specific fine-tuning.<br><br><strong>Multitask by design:</strong> special tokens in the decoder's prompt select the task — transcribe versus translate, language identification, timestamp prediction, voice activity. One model, one decoding format, many capabilities.<br><br><strong>The practical limitations that matter in production:</strong><br>• <em>Not streaming.</em> It needs a full 30-second window, so real-time use requires chunking with overlap, adding latency and boundary artifacts. For genuine streaming ASR, use a model designed for it (transducer-based).<br>• <em>Hallucination on silence</em> — a well-documented failure where it generates plausible text from non-speech audio. VAD gating before transcription is the standard fix and is close to mandatory.<br>• <em>No speaker diarization</em> — needs a separate model.<br>• <em>Domain vocabulary</em> — names and jargon need biasing via the initial prompt.` },

{ q: 'What is multimodal RAG, and how does it differ from text-only RAG?', tags: ['CRITICAL'], a: `Multimodal RAG retrieves and reasons over non-text content — images, charts, tables, video — alongside text.<br><br><strong>Two architectures, and the choice is the main decision:</strong><br><br><strong>Text-bridged (recommended default):</strong> at ingest, run a vision-language model over each image, chart, and table to produce a rich text description; also extract OCR text and captions. Embed <em>that text</em> with a strong text embedder into a single index. Retrieval stays purely textual — so hybrid search, BM25, and cross-encoder reranking all keep working. At answer time, pass the <em>original image</em> alongside its description so the model reasons over the artifact rather than the summary.<br><br><strong>Native multimodal embeddings:</strong> use CLIP-style models to put images and text in one vector space. Cleaner conceptually, but these embeddings are substantially weaker than text embedders for document retrieval, there is no lexical arm to compensate, and the tooling is less mature.<br><br><strong>What genuinely differs from text-only RAG:</strong> ingest becomes expensive (a VLM call per image, ASR per video), so it is a batch pipeline with checkpointing and it dominates the cost profile. Chunking must keep images with their captions and surrounding context. Tables must be preserved as structured data, never flattened into prose. And video needs segment-level indexing with timestamps so results deep-link to the moment.<br><br><strong>The most common failure is upstream:</strong> extracting figures and table structure from PDFs. Parsing quality, not retrieval, is usually the ceiling.` },

{ q: 'How do you build a system that processes both images and text?', tags: [], a: `<strong>Decide the interaction pattern first,</strong> since it determines everything else: are images an <em>input</em> to be understood (VQA, document processing), a <em>retrieval target</em> (search), or an <em>output</em> (generation)? These are three different systems.<br><br><strong>For understanding:</strong> a VLM takes image plus text prompt directly. The engineering work is around it — resolution handling (dynamic tiling for detail-heavy images), token budgeting (a high-res image can cost thousands of tokens, re-paid every turn if it stays in context), and extracting structured output rather than prose so downstream code can consume it.<br><br><strong>For retrieval:</strong> text-bridge at ingest — VLM descriptions plus OCR embedded with a text embedder — so your existing hybrid search and reranking keep working. Keep the original asset in object storage, referenced by id.<br><br><strong>The context-cost point people miss:</strong> in a multi-turn or agentic flow, do not keep raw images in the conversation. Extract what matters once (description, OCR text, structured fields), carry that forward, and re-attach the image only when a step genuinely needs to look again. Otherwise cost and attention dilution compound every turn.<br><br><strong>Validation is harder:</strong> you cannot string-match a claim about an image. Use structured extraction with a schema so outputs are checkable, and verify with a second model or against OCR where possible.<br><br><strong>Security:</strong> images are an injection vector — text rendered in an image is read as text. Apply the same untrusted-content handling you apply to retrieved web pages, including OCR-then-filter.` },

{ q: 'What are multimodal embeddings, and how are they used for cross-modal search?', tags: [], a: `Multimodal embeddings place different modalities in a <em>shared</em> vector space so a text query can retrieve images (or audio, or video) by proximity — no labels or captions needed at query time.<br><br><strong>How they are trained:</strong> contrastive learning across modalities. CLIP pairs image and text encoders on hundreds of millions of caption pairs, pulling matches together and pushing mismatches apart within each batch. ImageBind extends this to six modalities by binding each one to images as a hub, which yields emergent alignment between pairs never trained together (audio↔text, for instance).<br><br><strong>What they enable:</strong> text-to-image search over unlabeled collections, zero-shot classification (embed class names as text, take the nearest), reverse image search, duplicate detection, and cross-modal recommendation.<br><br><strong>Implementation is the same as text retrieval:</strong> embed the corpus offline into an ANN index, embed the query at runtime, nearest-neighbor search. Same index structures, same quantization tradeoffs.<br><br><strong>The limitation that determines architecture:</strong> CLIP-style embeddings were trained on short captions, so they capture coarse visual-semantic correspondence — not fine detail, compositional relations, counting, or text within images. For <em>document</em> retrieval they are markedly worse than a good text embedder, and there is no BM25 arm to compensate.<br><br><strong>So the production pattern is usually hybrid:</strong> text-bridged retrieval (VLM-generated descriptions embedded with a text model) as the primary arm, with a CLIP index as a secondary arm for genuine visual-similarity queries, fused with RRF.` },

{ q: 'How do you evaluate multimodal AI systems?', tags: [], a: `<strong>Evaluate per modality and per capability, not in aggregate</strong> — a single score hides that the system reads text in images well and cannot count objects at all.<br><br><strong>Capability-specific evaluation for VLMs:</strong> OCR and text reading, spatial reasoning ("what is to the left of X"), counting, chart and diagram interpretation, and fine detail. These have very different accuracy profiles on the same model, and knowing which one your task depends on tells you whether the model is viable.<br><br><strong>Established benchmarks:</strong> VQAv2 and GQA (visual question answering), TextVQA and DocVQA (text in images and documents), ChartQA, MMMU (expert-level multimodal reasoning), and POPE specifically for <em>object hallucination</em>. Treat these as coarse screening — contamination and saturation apply as they do for text benchmarks.<br><br><strong>Hallucination is the dominant failure mode</strong> and needs its own measurement. VLMs describe objects that are not present, particularly ones that commonly co-occur with what <em>is</em> present — a strong language prior overriding visual evidence. POPE and CHAIR quantify it directly; include it in your eval set explicitly.<br><br><strong>For generation</strong> (image, audio, video): FID and CLIPScore are standard but correlate imperfectly with human judgment, so pair them with human evaluation on prompt adherence, quality, and diversity.<br><br><strong>Cross-modal safety must be evaluated jointly.</strong> Testing text safety and image safety separately provably misses combination attacks — instructions rendered in an image bypass text filters entirely, and that is the point of the attack.` },

{ q: 'What are the challenges of real-time multimodal processing?', tags: ['TRADEOFF'], a: `<strong>The dominant constraint is that every modality adds a serial stage to the latency budget.</strong> A voice assistant needs ASR → LLM → TTS in under ~500ms end to end for conversation to feel natural; run those sequentially at full quality and you are at 2–3 seconds.<br><br><strong>The techniques that make the budget:</strong> <em>streaming at every stage</em> — partial ASR transcripts let the LLM begin prefill before the user stops speaking; LLM output streams sentence-by-sentence into streaming TTS so audio starts before generation finishes. <em>Speculative processing</em> on partial input, discarded if the user continues. Overlapping stages rather than pipelining them is the whole game.<br><br><strong>Vision-specific costs:</strong> images consume thousands of tokens, so prefill is expensive and TTFT suffers. Video is worse — you must sample frames rather than process all of them, which is a coverage/cost tradeoff you set explicitly (1–2 fps is typical for moderation, higher for action recognition).<br><br><strong>Synchronization</strong> across modalities with different natural rates (30fps video, 16kHz audio, sporadic text) requires buffering and alignment, and buffers add latency.<br><br><strong>Endpointing is an underrated failure source:</strong> deciding when the user finished speaking. Too aggressive cuts people off; too lax feels sluggish. Semantic endpointing beats silence thresholds.<br><br><strong>The architectural choice:</strong> cascaded pipelines are debuggable and let you swap components; end-to-end speech-to-speech models are lower latency and preserve prosody but are much harder to control and debug. Cascaded remains the pragmatic default.` },

{ q: 'How do you handle video understanding with AI?', tags: [], a: `<strong>The core problem is that video is enormous.</strong> A 10-minute clip at 30fps is 18,000 frames; encoding each as ~500 visual tokens is 9 million tokens. You cannot process it densely, so <em>every</em> video system is fundamentally a sampling and compression strategy.<br><br><strong>The standard pipeline:</strong> segment into scenes (shot-boundary detection), sample keyframes per scene, transcribe audio with ASR, run OCR on frames for on-screen text, and index at <em>segment</em> granularity with timestamps. Retrieval returns a segment that deep-links to the moment — indexing whole videos as single units makes them findable but unusable.<br><br><strong>Sampling rate is the key tradeoff:</strong> 1–2 fps suffices for topic understanding and moderation; action recognition and fast events need higher rates. Adaptive sampling — denser where visual change is high — is a good compromise.<br><br><strong>Audio is often the highest-value signal and the cheapest.</strong> For lectures, meetings, and most instructional content, the transcript carries most of the semantics at a tiny fraction of the cost of frame analysis. Start there.<br><br><strong>Temporal reasoning is where current models are weakest:</strong> ordering, causality, and counting events across time. Frame-sampled VLMs effectively see a slideshow, so they answer "what is in this video" well and "what happened before X" poorly. Purpose-built video models with temporal attention do better; be skeptical of claims otherwise.<br><br><strong>Ingest cost dominates the system's economics</strong> — batch pipeline, checkpointed, not inline.` },

{ q: 'What is visual question answering (VQA)?', tags: [], a: `VQA is answering natural-language questions about an image — the canonical multimodal reasoning task, requiring the model to ground language in visual evidence rather than pattern-match either alone.<br><br><strong>How modern systems do it:</strong> a VLM takes image plus question, encodes patches into the language model's embedding space, and generates an answer. Earlier systems used a classification head over a fixed answer vocabulary; generative VLMs produce open-ended text, which is more flexible and harder to evaluate.<br><br><strong>The failure mode that defines the field: language priors.</strong> Models learn to answer from the question alone. "What color is the banana?" → "yellow" without looking. "How many legs does the dog have?" → "four." VQAv2 was specifically constructed with complementary image pairs having different answers to the same question, precisely to penalize this — and it is still the dominant error source.<br><br><strong>Related — object hallucination:</strong> VLMs describe objects that are not present, especially objects that commonly co-occur with what <em>is</em> present. POPE measures this directly.<br><br><strong>Where VQA is genuinely hard:</strong> counting, spatial relations, reading small text, and any question requiring precise detail — all limited by fixed-resolution patching that discards fine information.<br><br><strong>Practical mitigations:</strong> higher-resolution tiling for detail tasks, explicit grounding instructions ("answer only from what is visible; say 'cannot determine' otherwise"), and a separate OCR path for text-heavy images rather than relying on the VLM to read.` },

{ q: 'What is document understanding, and how do models parse layouts?', tags: ['CRITICAL'], a: `Document understanding extracts structure and meaning from documents where <em>layout carries semantics</em> — tables, forms, invoices, contracts. Position is information: a number's meaning depends on which column and row it sits in.<br><br><strong>Why plain text extraction fails:</strong> PDF stores glyph positions, not structure. There is no reliable notion of a table, a column, or reading order in the file. Naive extraction interleaves multi-column text and flattens tables into unusable token soup. This is the ceiling on most enterprise document pipelines, and it is upstream of everything else.<br><br><strong>Approaches, in evolution order:</strong><br>• <em>Geometric heuristics</em> (pdfplumber, PyMuPDF) — reconstruct reading order and detect tables from coordinates. Works on clean digital-native PDFs.<br>• <em>Layout-aware models</em> (LayoutLM family, Donut) — jointly encode text, its 2D position, and image features, so the model learns that a value's spatial relationship to a label matters. Donut is OCR-free, going directly from image to structured output.<br>• <em>Vision-language models</em> — render the page as an image and have a VLM transcribe it to structured markdown. This is the current pragmatic answer: it handles multi-column flow, merged cells, and figures far better than heuristics, at the cost of one VLM call per page.<br><br><strong>Practical guidance:</strong> preserve tables as markdown or HTML, never as prose; chunk on the recovered structure (sections, headings) rather than character counts; and <strong>read the extracted output for a sample of documents before building anything on top</strong> — most "our RAG is bad" investigations end at page one of the parser output.` },

{ q: 'How do you fine-tune a vision-language model?', tags: [], a: `<strong>Fine-tune in stages, mirroring how these models are trained.</strong> The standard recipe: freeze the vision encoder and the LLM, train only the projection layer first to align modalities, then unfreeze and instruction-tune. For domain adaptation you usually only need the second stage.<br><br><strong>Use PEFT.</strong> LoRA on the language model's attention projections, and often on the vision encoder's too. Full fine-tuning of a VLM is expensive and, as with text models, rarely necessary — and freezing the base resists catastrophic forgetting of general visual understanding.<br><br><strong>What to freeze depends on the gap:</strong> if the model understands your images but formats output wrong, train only the LLM side. If the visual domain is genuinely alien — medical imaging, satellite, industrial inspection — the vision encoder needs adaptation too, and that is a much larger undertaking.<br><br><strong>Data:</strong> (image, instruction, response) triples matching your task and output format exactly. Quality dominates quantity, as with text — a few thousand well-constructed examples typically suffice for format and domain adaptation. Include negative and abstention cases ("cannot determine from this image") or the model will always answer.<br><br><strong>Practical details that bite:</strong> resolution must match training and inference; image preprocessing (resize, normalization, tiling strategy) must be identical, and mismatches here silently degrade everything. Visual tokens consume most of the sequence budget, so batch sizes are small and gradient accumulation is usually necessary.<br><br><strong>Evaluate for regression</strong> on general visual understanding, not just your task — and specifically for object hallucination, which fine-tuning can worsen.` },

{ q: 'What are the latency and cost considerations for multimodal AI in production?', tags: ['TRADEOFF'], a: `<strong>Images are expensive in tokens, and that is the dominant cost fact.</strong> A high-resolution image can consume thousands of input tokens — often more than the entire text prompt. Resolution drives this quadratically, so the resolution decision <em>is</em> the cost decision.<br><br><strong>The compounding failure in multi-turn and agentic flows:</strong> if an image stays in the conversation, you re-pay for it at <em>every subsequent step</em>. Extract what you need once (description, OCR text, structured fields), carry that forward, and re-attach the image only when a step genuinely needs to look again. Teams that keep three screenshots in history are paying for them dozens of times.<br><br><strong>Latency:</strong> prefill scales with visual token count, so TTFT degrades sharply with image size and count. For interactive use, downscale aggressively and only escalate resolution when the task demands detail.<br><br><strong>Ingest cost dominates multimodal RAG.</strong> A VLM call per image, ASR per video, OCR per page — this is a batch pipeline with checkpointing, and it typically exceeds serving cost by a wide margin. Budget it as a one-time capital cost per document.<br><br><strong>Optimization levers:</strong> tiered resolution (thumbnail first, full-res only if needed); cache VLM descriptions since images rarely change; use a cheap specialized model (OCR, a small classifier) instead of a frontier VLM where the task is narrow — OCR for text extraction is orders of magnitude cheaper and more accurate than asking a VLM to read.<br><br><strong>Generation</strong> (image, video) is GPU-time-bound and belongs in an async job queue, never a synchronous request path.` },

{ q: 'How do you handle multimodal content moderation?', tags: ['CRITICAL'], a: `<strong>The defining requirement: moderate modalities jointly, not separately.</strong> Testing text safety and image safety independently provably misses combination attacks — and those are the ones adversaries use. Benign text plus a benign-looking image can combine into a harmful request that neither filter catches.<br><br><strong>The most important specific case: text rendered in images.</strong> A vision model reads it as text, so any filtering applied only to the typed message is trivially bypassed by screenshotting the prompt. <strong>Run OCR on every image and apply your text safety filters to the extracted text</strong> — this single control closes the most common bypass and is routinely missing.<br><br><strong>Layer by cost:</strong> hash matching against known violating content (near-zero cost, catches known material) → fast per-modality classifiers → a multimodal model for ambiguous cases → human review. At volume you cannot run expensive models on everything.<br><br><strong>Modality-specific needs:</strong> images (NSFW, violence, likeness detection); video (per-frame <em>and</em> temporal — content can be benign frame-by-frame and violating as a sequence); audio (transcribe, then apply text moderation, plus audio-specific signals).<br><br><strong>Context changes classification:</strong> a medical image is clinical or explicit depending on the surface it appears on. Feed surrounding context to the classifier.<br><br><strong>Human review is part of the system:</strong> prioritized by severity, with wellbeing safeguards for graphic content, and decisions feeding back as training labels.<br><br><strong>Measure per modality and per market</strong> — aggregate accuracy hides that video moderation is failing.` },

{ q: 'What is text-to-video generation, and what are the current approaches?', tags: [], a: `Text-to-video generates a video clip from a text prompt. The dominant approach is <strong>latent diffusion with a spatiotemporal architecture</strong> — increasingly a Diffusion Transformer (DiT) rather than a U-Net — operating on a video latent compressed in both space and time by a 3D VAE.<br><br><strong>The core technical problem is temporal consistency.</strong> Generating frames independently produces flickering incoherence; objects must persist, move plausibly, and obey rough physics. Solutions: 3D (spatiotemporal) attention so the model sees all frames jointly, and training on video rather than images. Sora's reported approach treats video as sequences of spacetime patches, which lets one model handle variable durations, resolutions, and aspect ratios — a genuinely useful generalization.<br><br><strong>The economics dominate every design decision.</strong> Video is by far the most expensive generative modality — minutes of GPU time for seconds of output. That forces multi-stage generation: produce low resolution and frame rate first, let the user preview and approve, then spatially upsample and temporally interpolate only on commitment. That preview gate is the single most important cost control.<br><br><strong>Practical constraints:</strong> clip length is limited (seconds, not minutes) because cost and consistency both degrade with duration; long-form requires generating segments and stitching, with drift across boundaries.<br><br><strong>Serving:</strong> strictly async job queue with progress streaming, reserved GPU capacity (cold starts make reactive scaling useless), and honest ETAs.<br><br><strong>Provenance is mandatory</strong> — C2PA metadata and watermarking, given deepfake risk.` },

{ q: 'Explain early fusion vs late fusion.', tags: ['TRADEOFF'], a: `<strong>Early fusion</strong> combines modalities at the <em>input or feature</em> level, before the main processing. Concatenate or project embeddings into a shared sequence and let a single model attend across all of them jointly from layer one.<br><br><strong>Late fusion</strong> processes each modality independently through its own model and combines only at the <em>decision</em> level — averaging predictions, weighted voting, or a small combiner network.<br><br><strong>Early fusion — pros and cons:</strong> captures fine-grained cross-modal interaction, since every layer can relate a word to an image region. That is essential for tasks where meaning emerges from the combination (VQA, sarcasm detection, grounded reasoning). Costs: requires aligned, complete data — a missing modality breaks the input; and it is more expensive to train.<br><br><strong>Late fusion — pros and cons:</strong> modular (train and swap each encoder independently), robust to missing modalities (drop that branch), efficient, and easy to debug per modality. But it cannot capture interactions, because each branch never sees the others. Two independently correct predictions cannot resolve a case where the modalities <em>together</em> mean something neither means alone.<br><br><strong>Intermediate/hybrid fusion</strong> combines at intermediate layers via cross-attention — Flamingo-style — and is what most modern systems actually use, getting interaction depth without full early-fusion rigidity.<br><br><strong>How I'd choose:</strong> late fusion when modalities are independently informative and robustness or modularity matters (multimodal moderation, where each signal is separately sufficient). Early or intermediate fusion when the task <em>requires</em> grounding one modality in another — which is most interesting multimodal reasoning.` },

{ q: 'Your VLM generates factually incorrect image descriptions. How do you fix it?', tags: ['CRITICAL'], a: `<strong>Diagnose the failure type first, because they have different fixes:</strong><br>• <em>Object hallucination</em> — describing objects that are not present, especially ones that commonly co-occur with what is present. This is a language prior overriding visual evidence, and it is the most common failure.<br>• <em>Detail errors</em> — misreading text, miscounting, wrong colors or spatial relations. Usually a resolution problem.<br>• <em>Confabulated specifics</em> — inventing names, dates, or brands not visible.<br><br><strong>For resolution-driven errors:</strong> increase input resolution or use dynamic tiling. Small text and fine detail are simply discarded by fixed-resolution patching, and no prompting recovers information that never reached the model. For text-heavy images, <strong>use a dedicated OCR pass</strong> and give the model the extracted text — orders of magnitude more accurate and cheaper than asking a VLM to read.<br><br><strong>For hallucination:</strong> prompt for grounding explicitly ("describe only what is visible; say 'cannot determine' if unclear") with few-shot examples that <em>demonstrate</em> abstaining — without demonstrations the instruction is largely ignored. Set temperature to 0. Ask for structured output with a schema rather than free-form prose, which constrains invention.<br><br><strong>Verify rather than trust:</strong> cross-check claims against OCR output, an object detector, or a second model. For counting and detection, a specialized model beats a VLM.<br><br><strong>Measure it:</strong> POPE and CHAIR quantify object hallucination directly. Build an eval set with known ground truth and adversarial cases (objects that <em>should</em> be there by context but are not) — that specific test is what catches prior-driven hallucination.` },

{ q: 'Your VLM answers single-image questions but fails on multi-page documents. How do you fix it?', tags: [], a: `<strong>The cause is usually token budget and attention dilution,</strong> not a reasoning limitation. Each page consumes hundreds to thousands of visual tokens, so a 30-page document either exceeds the context window or fills it with content the model attends to poorly — the lost-in-the-middle effect applies to visual tokens too.<br><br><strong>The fix is retrieval, not more context.</strong> Treat it as multimodal RAG: parse and index each page separately (VLM-generated description plus OCR text plus extracted tables), retrieve only the relevant pages for the question, then pass those pages' images to the VLM for the actual reasoning. This is both cheaper and more accurate than stuffing the whole document.<br><br><strong>Preserve structure during indexing:</strong> section headings, page numbers, and cross-references. Document questions frequently require knowing <em>where</em> something appeared ("in the pricing section"), and flat page images lose that.<br><br><strong>Handle cross-page dependencies explicitly,</strong> which is where naive retrieval fails: a table spanning pages, a defined term used 20 pages later, or a total that references line items elsewhere. Mitigate by retrieving neighboring pages alongside hits, and by resolving defined terms during preprocessing.<br><br><strong>For text-dominant documents, do not use the VLM as an OCR engine.</strong> Run a proper layout-aware parse or a VLM transcription pass at ingest, then do retrieval and reasoning over text. This is faster, cheaper, and more accurate than repeated visual reading.<br><br><strong>For genuinely holistic questions</strong> ("summarize this contract"), use map-reduce over pages with verification at the page level before reduction.` },

{ q: 'Your multimodal LLM ignores the image and answers from text alone. How do you fix it?', tags: ['CRITICAL'], a: `<strong>This is the language-prior problem — the model answers from its text knowledge because that path is stronger than the visual one.</strong> "What color is the banana?" → "yellow" without looking. Verify it is happening by running with the image removed: if the answer is identical, the image contributed nothing.<br><br><strong>Check the plumbing first,</strong> since a surprising fraction of these cases are bugs: is the image actually reaching the model in the right format and order? Is it being silently downscaled or dropped by a size limit? Look at the literal request payload.<br><br><strong>Prompt-level fixes:</strong> instruct explicitly to answer from the image ("base your answer only on the provided image; if it is not visible, say so"), and place the question <em>after</em> the image. Ask the model to first describe what it observes, then answer — forcing an explicit grounding step measurably reduces prior-driven answers. Provide few-shot examples where the visually-correct answer contradicts the expected prior.<br><br><strong>Make the question un-answerable from text alone:</strong> "what color is the object in the top-left" is harder to guess than "what color is the banana."<br><br><strong>Structural fixes:</strong> higher resolution if the relevant detail is being lost — the model may be ignoring the image because it genuinely cannot see the detail. And for text in images, extract with OCR and pass it explicitly rather than relying on visual reading.<br><br><strong>If it persists, fine-tune</strong> on examples where the answer requires the image, including counterfactual pairs where the same question has different answers for different images. That is exactly how VQAv2 was constructed to penalize this behavior.` },

{ q: 'Your diffusion model ignores precise control requirements in prompts. How do you improve controllability?', tags: ['TRADEOFF'], a: `<strong>Understand the limit first: text is a weak control signal for spatial precision.</strong> Cross-attention conditioning captures <em>what</em> should appear far better than <em>where</em> and <em>how many</em>. Prompt engineering will not fix compositional, spatial, or counting requirements — that is an architectural limitation, and the answer is a different conditioning channel.<br><br><strong>Use structural conditioning, which is the real fix:</strong><br>• <strong>ControlNet</strong> — condition on an explicit structural input: edge map, depth map, pose skeleton, segmentation mask, or scribble. This gives precise spatial control that text cannot express, and it is the standard answer for layout requirements.<br>• <strong>Regional prompting / attention control</strong> — bind specific prompt terms to specific image regions.<br>• <strong>IP-Adapter</strong> for conditioning on a reference image's style or subject.<br>• <strong>Inpainting</strong> — fix the parts that are correct, regenerate only the rest.<br><br><strong>Prompt-level levers, which help but are secondary:</strong> raise the classifier-free guidance scale for stronger prompt adherence — but note this trades against diversity and pushed too high produces over-saturated, artifacted images. Put important terms early. Use negative prompts to suppress unwanted elements.<br><br><strong>For a specific recurring subject or style, fine-tune:</strong> LoRA or DreamBooth on reference images gives far more reliable control than any prompt.<br><br><strong>The workflow answer:</strong> generate several candidates and select, or iterate with inpainting. Single-shot precise control is not what diffusion models do well; iterative refinement with structural conditioning is.` },

{ q: 'Your diffusion model generates sharp but repetitive images. How do you balance quality and diversity?', tags: ['TRADEOFF'], a: `<strong>The most likely cause is guidance scale set too high.</strong> Classifier-free guidance extrapolates away from the unconditional prediction toward the prompt — raising it improves prompt adherence and perceived sharpness while <em>collapsing diversity</em>, because it pushes every sample toward the same high-probability mode. Sharp and repetitive is the exact signature. Lower the guidance scale first; it is one parameter and it usually resolves the complaint.<br><br><strong>Other causes to check:</strong><br>• <em>Fixed or insufficiently varied seeds</em> — trivially checked, and a common oversight in batch generation pipelines.<br>• <em>Over-specific prompts</em> that leave little room for variation. Paraphrase or randomize non-essential prompt elements across a batch.<br>• <em>Over-fine-tuned LoRA</em> — a LoRA trained too long on a small set collapses onto its training distribution. Lower the LoRA weight, or retrain with fewer steps and more varied data.<br>• <em>Sampler choice</em> — ancestral samplers (Euler a, DPM++ SDE) inject noise at each step and produce more variation than deterministic ones (DDIM, Euler); switching is a cheap experiment.<br><br><strong>The tradeoff to state explicitly:</strong> this is the fidelity/diversity frontier, and guidance scale is the dial that moves you along it. There is no setting that gives both — you choose an operating point per use case. Product photography wants high guidance and low diversity; concept exploration wants the opposite.<br><br><strong>Measure it:</strong> FID captures distribution match, but for diversity specifically use recall/coverage metrics or LPIPS distance between samples from the same prompt. "It looks repetitive" is not actionable; a diversity number is.` },

{ q: 'Your diffusion model takes too long per image. How do you speed up sampling?', tags: ['CRITICAL'], a: `<strong>Step count is the dominant cost, and it is nearly linear</strong> — each denoising step is a full forward pass. So the highest-leverage question is: how few steps can you use?<br><br><strong>Better samplers first (free):</strong> DPM++ 2M Karras and similar higher-order solvers reach comparable quality in 20–25 steps where DDIM needed 50. Switching sampler and schedule is a config change with no quality loss, and it is the first thing to try.<br><br><strong>Distillation (the big win):</strong> Latent Consistency Models, SDXL-Turbo, and adversarial diffusion distillation train a student to take much larger steps, reaching usable quality in <strong>1–4 steps</strong> — an order of magnitude faster. The tradeoff is some quality and diversity loss, and you need a distilled checkpoint for your base model. This is what makes real-time and interactive generation possible.<br><br><strong>Standard inference optimizations:</strong> fp16/bf16, attention optimizations (Flash Attention, xFormers), torch.compile, TensorRT, and channels-last memory format. Collectively often 2x with no quality change.<br><br><strong>Resolution:</strong> cost scales with latent area. Generate at lower resolution and upscale with a fast super-resolution model — frequently faster <em>and</em> better than generating natively at high resolution.<br><br><strong>Batching:</strong> diffusion batches extremely well since every image takes the same number of steps. If you are generating one image per GPU call, you are leaving most of your throughput unused.<br><br><strong>Product-level:</strong> a fast draft tier for preview and a slow high-quality tier on commit — this gives perceived speed while preserving final quality, and it cuts total compute because most drafts are discarded.` },
]};
