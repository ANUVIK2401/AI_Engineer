/* Behavioral and Scenario-Based Questions — 22 questions */
window.QA_SECTION = {
  id: '14',
  slug: 'behavioral',
  title: 'Behavioral & Scenario-Based',
  blurb: 'Judgment, tradeoffs, stakeholder communication — where senior candidates are actually separated from mid-level.',
  questions: [
{ q: 'What is AI Engineering, and how does it differ from ML Engineering?', tags: ['CRITICAL'], a: `<strong>ML Engineering</strong> centers on the model as the artifact you produce: data collection and labeling, feature engineering, training, hyperparameter tuning, and deploying a model you own.<br><br><strong>AI Engineering</strong> centers on building <em>systems</em> around foundation models you did not train. The artifacts you iterate on are prompts, retrieval corpora, tool definitions, and evaluation sets — not weights.<br><br><strong>What actually changes in the work:</strong><br>• The expensive step (pretraining) already happened, so the bottleneck moves from training to <em>context, evaluation, and orchestration</em>.<br>• Data work shifts from labeling to curation and eval-set design.<br>• Your core dependency is a third-party model that can change without a deploy on your side — so version pinning and continuous evaluation become reliability requirements.<br>• Failure modes are new: hallucination, prompt injection, jailbreaks — and they fail <em>silently</em>, returning a 200 with wrong content.<br>• Cost is per-request and variable, so token budgets are a first-class engineering concern rather than a fixed infrastructure line.<br><br><strong>What carries over unchanged:</strong> data quality determines outcomes, evaluation discipline separates working systems from demos, and distribution shift is still the thing that breaks you in production.<br><br><strong>The framing I'd offer:</strong> AI Engineering is closer to distributed systems engineering than to data science. Most of the hard problems are latency, cost, reliability, and measurement — with a probabilistic component in the middle.` },

{ q: 'How do you decide whether a problem needs AI or traditional software?', tags: ['CRITICAL'], a: `<strong>Default to traditional software.</strong> It is deterministic, testable, cheaper, faster, and debuggable. AI should have to justify itself.<br><br><strong>Use AI when the problem has these properties:</strong> the input is unstructured (natural language, images, audio); the rules are numerous, fuzzy, or change constantly; a human could do it but you cannot write down how; and — critically — <strong>the cost of being wrong sometimes is acceptable</strong>.<br><br><strong>Use traditional software when:</strong> the rules are enumerable (a tax calculation, a routing table, an eligibility check); you need exact reproducibility or auditability; correctness is required rather than desirable; or the operation is arithmetic. Asking an LLM to sum a column is a design error — it is slower, more expensive, and sometimes wrong at a task a database does perfectly.<br><br><strong>The most common mistake I see</strong> is using an LLM as a component in a deterministic pipeline where a lookup, a regex, or a SQL query would do. The second is using it for arithmetic and aggregation.<br><br><strong>The hybrid is usually the right answer:</strong> use the LLM to convert unstructured input into structured data, then make the actual decision with deterministic code. That gives you the flexibility of language understanding with the auditability and correctness of ordinary software — and it is what makes regulated AI systems defensible.<br><br><strong>The question I'd ask a stakeholder:</strong> "what happens when it's wrong 5% of the time?" If the answer is unacceptable, either add a human gate or do not use AI for that step.` },

{ q: 'How do you measure the ROI of an AI feature?', tags: [], a: `<strong>Define the value mechanism first,</strong> because the measurement follows from it. AI features create value in one of three ways: <em>cost reduction</em> (fewer support hours, less manual review), <em>revenue</em> (conversion, retention, new capability), or <em>enablement</em> (something previously impossible).<br><br><strong>Cost side — count everything, not just tokens:</strong> inference (input and output separately, as a distribution since the tail dominates), embedding and ingest, evaluation runs (a real line item for actively developed systems), engineering build and maintenance, and the human-review layer that most AI features actually require.<br><br><strong>Measure with a controlled experiment.</strong> A/B test or a holdout group — observational before/after comparisons cannot separate your feature's effect from seasonality, other launches, or the team simply paying more attention. This is the discipline that separates a real ROI claim from a story.<br><br><strong>Include second-order costs honestly:</strong> the escalations a wrong answer generates, trust damage, and the review burden. A support bot deflecting 40% of tickets but producing wrong answers on 5% can be net-negative once you count the escalated, now-angrier customers.<br><br><strong>Report cost per successful outcome, not cost per request.</strong> A feature at $0.30/request that resolves the task beats one at $0.05/request that needs three attempts and a human.<br><br><strong>And be willing to conclude it is negative.</strong> The credibility you get from killing a feature that does not pay for itself is what makes your next ROI claim believable.` },

{ q: 'How do you handle hallucinations when they occur in production?', tags: ['CRITICAL'], a: `<strong>Immediate:</strong> assess scope from logs — how many users, over what period, and how severe. If the harm is material, disable or degrade the feature via feature flag. This is why kill switches are a design requirement, not a nicety.<br><br><strong>Then diagnose the class, because the fixes differ:</strong><br>• <em>Retrieval failure</em> — the right information was never retrieved, so the model filled the gap. Fix retrieval, not the prompt. Check whether the source document is retrievable at all.<br>• <em>Grounding failure</em> — the right context reached the model and it answered from parametric knowledge anyway. Fix with explicit grounding instructions and a stronger abstention path.<br>• <em>No abstention path</em> — the context was partially relevant so the model extrapolated. Add an explicit refusal option with few-shot examples demonstrating it.<br>• <em>Chunking artifact</em> — a claim was severed from its qualifier, so the model faithfully reported a misleading fragment. Looks like hallucination, is actually an ingest bug.<br><br><strong>Then add detection, which is the durable fix:</strong> require verbatim supporting quotes and verify they exist in the source (deterministic and free), or run entailment checks per claim. This converts hallucination from an unmeasured risk into a detected event you can alert on.<br><br><strong>Close the loop:</strong> the failing case becomes a permanent regression test. And be honest with stakeholders — you reduce and detect hallucination; you do not eliminate it. Systems that must be right need a verification layer or a human.` },

{ q: 'How do you decide between an LLM API and self-hosting?', tags: ['TRADEOFF'], a: `<strong>Start on an API.</strong> Self-hosting before you know your workload shape is premature optimization — you will provision for traffic you cannot predict and spend engineering time on a serving stack instead of on the product.<br><br><strong>Move to self-hosting when one of these fires:</strong><br>• <em>Regulatory or data-residency requirements</em> that an API cannot satisfy. This is the most common decisive reason, and it is not negotiable.<br>• <em>Sustained high volume with predictable traffic</em>, where GPU amortization beats per-token pricing. Compute your cost per million tokens self-hosted (GPU hourly ÷ achievable tokens/hour) against API pricing — and note the critical variable is <strong>utilization</strong>. A GPU at 20% utilization has 5x the effective cost per token, so bursty traffic destroys the economics.<br>• <em>Latency guarantees</em> an API cannot meet.<br>• <em>A narrow task where a fine-tuned small model matches a frontier model</em> at a fraction of the cost — usually the strongest economic argument, and it is really a model-size decision rather than a hosting one.<br><br><strong>Count the costs people omit from the self-hosted side:</strong> engineering time to build and operate the serving stack, on-call burden, over-provisioning for peak, idle capacity, and the quality gap (an open model may need more retries to reach the same result).<br><br><strong>The pragmatic middle:</strong> self-host a fine-tuned small model for the high-volume easy majority, escalate the hard tail to an API. That captures most of the cost saving without capping quality.` },

{ q: 'How do you manage stakeholder expectations for AI projects?', tags: [], a: `<strong>Set the probabilistic framing early and concretely.</strong> The core misunderstanding is that stakeholders expect software behavior — deterministic, testable, either working or broken. Say plainly: this system will be wrong some percentage of the time, that percentage can be measured and reduced but not eliminated, and the design question is what happens when it is wrong.<br><br><strong>Replace demos with measurements.</strong> Demos are the single largest source of expectation inflation — a curated demo shows the best case, and stakeholders anchor on it. Build the eval set early and report numbers from it, including failure examples. Showing three real failure cases alongside the success rate does more for calibration than any explanation.<br><br><strong>Commit to a measurable target, not a capability.</strong> "85% of extractions auto-accepted with the rest routed to review" is a commitment you can be held to and can verify. "It will read documents" is not.<br><br><strong>Be explicit about the human layer.</strong> Most AI features need review, escalation, or appeals, and that has an operational cost. Stakeholders who budget for a fully automated system and get one needing 20% human review will consider it a failure regardless of its actual quality.<br><br><strong>Report cost and latency alongside quality</strong> from the start, so nobody is surprised by the bill.<br><br><strong>And update forecasts as evidence arrives.</strong> The credibility that comes from saying "the eval shows this won't hit the bar; here's what I'd do instead" is what makes your later claims believable.` },

{ q: 'Describe your approach to debugging a poor-performing RAG system.', tags: ['CRITICAL'], a: `<strong>Debug in pipeline order,</strong> because most teams start at the prompt and waste days when the failure is three stages upstream.<br><br><strong>1. Read the extracted text.</strong> Before anything else, look at what actually landed in the index for a failing query. Broken PDF extraction, lost table structure, and interleaved multi-column text are extremely common, and they cap everything downstream. Many investigations end here.<br><br><strong>2. Check whether the correct chunk is retrievable at all.</strong> Search for it directly. If it never surfaces, the problem is embeddings, chunking, or a metadata filter silently excluding it. If it surfaces at rank 40, it is a ranking problem and reranking fixes it. <em>This single check separates two very different fixes</em> and is the highest-value diagnostic step.<br><br><strong>3. Measure retrieval in isolation.</strong> Build a small labeled set of query→relevant-document pairs and compute recall@k. Recall is the hard ceiling — anything not retrieved is unanswerable no matter how good the prompt is.<br><br><strong>4. Only then look at generation.</strong> If recall is high and answers are still wrong, inspect the <em>fully rendered prompt</em> — not the template. Was the chunk truncated by a token budget? Buried in the middle among 40 others? That is where "the model ignored the context" bugs actually live.<br><br><strong>The instrumentation that makes this tractable:</strong> log query, retrieved chunk ids with scores, the rendered prompt, and the answer for every request. Without that trace you are guessing.<br><br><strong>My prior:</strong> most RAG failures are retrieval failures, and most retrieval failures are parsing or chunking failures.` },

{ q: 'How do you stay current with the rapidly evolving AI landscape?', tags: [], a: `<strong>Be honest that you filter aggressively</strong> — the field produces more output than anyone can absorb, and the useful skill is triage, not consumption.<br><br><strong>What I actually track:</strong> primary sources for things that change my decisions — model and provider release notes and pricing changes (these directly affect architecture and cost), major papers that shift practice rather than incremental benchmark gains, and the release notes for tools I depend on (vLLM, serving frameworks, key libraries).<br><br><strong>The filter I apply:</strong> does this change something I would do differently tomorrow? Most announcements do not. A new benchmark SOTA rarely changes an application architecture; a 10x price change or a new serving technique does.<br><br><strong>How I actually learn things:</strong> building with them. Reading about speculative decoding gives you the concept; implementing it teaches you the acceptance-rate dependency that determines whether it helps your workload. I keep a small set of evaluation tasks and run new models against them, which is far more informative than reading benchmark tables.<br><br><strong>What I deliberately ignore:</strong> benchmark leaderboards (contaminated and saturated), launch hype, and anything without reproducible results.<br><br><strong>The meta-point worth making:</strong> the fundamentals move slowly. Attention, retrieval, evaluation discipline, and the latency/cost/quality tradeoff have been stable for years — what changes is which model is best this quarter. Depth on the fundamentals transfers; breadth on model announcements expires in weeks.` },

{ q: 'How do you balance innovation with reliability?', tags: ['TRADEOFF'], a: `<strong>Separate the two by blast radius rather than by policy.</strong> Innovation belongs where failure is cheap and reversible; reliability discipline belongs where it is not.<br><br><strong>Concretely:</strong> new models, new techniques, and architectural experiments go behind feature flags, in shadow mode, or on internal traffic first. The production path changes only when an eval says it should — and rolls out canary-first with automatic rollback.<br><br><strong>The mechanism that makes both possible is the eval suite.</strong> Without it, every change is a risk and teams become conservative by necessity — you cannot tell whether a new model is better, so you do not adopt it. With a good eval set, you can evaluate a new model in an afternoon and know whether it is safe. <em>Evaluation infrastructure is what buys you the ability to move fast</em>, which is the counterintuitive point worth making.<br><br><strong>Version pinning is the other half.</strong> Pin models explicitly so innovation is a decision you make, not something that happens to you when a provider updates an endpoint.<br><br><strong>On adoption timing:</strong> I am deliberately slow on new frameworks and fast on new models. Frameworks impose architecture and are expensive to unwind; models sit behind an abstraction layer and are cheap to swap if you built that layer. That asymmetry lets you capture most of the innovation upside with little of the lock-in risk.<br><br><strong>The judgment call I'd state:</strong> reliability is not the opposite of speed. Most outages I have seen came from changes shipped without measurement, not from changes shipped quickly.` },

{ q: 'Tell me about a challenging AI project. Problem, approach, tradeoffs, outcome.', tags: ['CRITICAL'], a: `<strong>Structure the answer as Situation → Approach → Tradeoffs → Outcome → What I'd do differently.</strong> Interviewers are testing whether you reason about tradeoffs, not whether the project succeeded.<br><br><strong>A strong shape, as an example:</strong><br><br><em>Situation:</em> a document QA system where users reported wrong answers on roughly a fifth of queries. The team had been iterating on prompts for weeks with no improvement.<br><br><em>Approach:</em> instrumented the pipeline before changing anything — logged retrieved chunk ids with scores and the rendered prompt. Built a 120-query labeled set and measured retrieval recall separately from generation quality. Recall@5 was 61%, which capped everything downstream. The prompt was never the problem.<br><br><em>Tradeoffs:</em> adding a cross-encoder reranker cost ~120ms p95, which mattered on an interactive path. Chose to accept it after establishing the latency budget with the PM, and offset it by cutting k from 20 to 5 — which also reduced token cost.<br><br><em>Outcome:</em> recall@5 to 89%, wrong-answer rate to about 6%, cost down roughly 40% from the smaller context.<br><br><em>What I'd do differently:</em> build the eval set first. Weeks of prompt iteration happened because nobody could measure which half was broken.<br><br><strong>What makes this answer land:</strong> a measured diagnosis rather than a guess, a named tradeoff with a decision and a reason, quantified outcomes, and a genuine lesson.` },

{ q: 'How would you handle biased or harmful outputs in production?', tags: ['CRITICAL'], a: `<strong>Immediate:</strong> assess severity and scope from logs, and if harm is material, disable or restrict the feature via kill switch while investigating. Do not debug in production while users are being harmed.<br><br><strong>Then determine the mechanism, since the fixes differ entirely:</strong> is it the base model's behavior, your prompt inducing it, retrieved content containing it, a fine-tuning dataset that taught it, or a guardrail gap that let it through?<br><br><strong>Short-term mitigation:</strong> tighten output classifiers, add the failure pattern to blocklists, and route the affected query class to human review. These are stopgaps, and I would say so rather than presenting them as fixes.<br><br><strong>Root-cause work:</strong> if the bias is in fine-tuning data, that is a data audit and retraining from base — you cannot subtract a learned pattern by training more. If it is base-model behavior, the fix is at the system layer: guardrails, constrained output, or a different model. If it is in the retrieval corpus, that is a content problem and the durable fix is upstream.<br><br><strong>Measure it properly:</strong> disaggregated evaluation by group <em>and intersection</em>, plus counterfactual testing (identical inputs varying only the demographic signal) to establish whether the effect is causal. Add the case as a permanent regression test.<br><br><strong>The process points that matter:</strong> notify affected users where warranted, involve legal and comms early rather than after, and run a blameless post-mortem focused on why evaluation and monitoring did not catch it — that is almost always the real finding.` },

{ q: 'How do you approach cost optimization when over budget?', tags: ['CRITICAL'], a: `<strong>Attribute before optimizing.</strong> Split spend by feature, by input versus output tokens, and look at the <em>distribution</em> rather than the mean. Cost per request is heavily long-tailed, and typically a small fraction of requests — runaway agent trajectories, huge contexts, retry storms — drives most of the bill. Capping that tail is often the single largest win and requires no quality tradeoff.<br><br><strong>Then take the free wins first, in this order:</strong><br>• <strong>Prompt caching</strong> — reorder prompts so static content (system prompt, tools, examples) comes first. Up to ~90% input cost reduction, zero quality cost, and it improves TTFT. Usually the best change available.<br>• <strong>Cap max output and max context</strong> as hard rails.<br>• <strong>Batch API</strong> (~50% off) for anything latency-tolerant, including your own eval runs.<br>• <strong>Exact-match caching</strong> for repeated queries.<br><br><strong>Then the changes that trade off, each gated on the eval set:</strong> model routing (send the easy majority to a small model — usually the largest structural saving), reranking so you can cut k from 20 to 5 (cheaper <em>and</em> better, one of the rare wins on both axes), and fine-tuning a small model for a high-volume narrow task.<br><br><strong>The discipline that makes this safe:</strong> every change runs against the eval set before shipping. Cost optimization without a quality gate reliably produces something cheaper and worse — and you find out from users.<br><br><strong>And be willing to say the feature does not pay for itself.</strong> That is sometimes the honest conclusion.` },

{ q: 'Describe choosing between accuracy and latency.', tags: ['TRADEOFF'], a: `<strong>Frame it as an operating point on a measured curve, not a binary choice.</strong> The answer interviewers want is a decision process, not a preference.<br><br><strong>How I'd approach it:</strong> establish the latency budget from the <em>user's</em> requirement first — autocomplete needs ~100ms, conversational needs ~1s to first token, an agentic task tolerates 30s if progress is visible. Then plot quality against p95 latency for each configuration on the eval set and choose the knee deliberately.<br><br><strong>A concrete example:</strong> on a document QA feature, adding a cross-encoder reranker raised recall@5 from 61% to 89% and cost ~120ms p95 on an interactive path. I took it — the wrong-answer rate was the user-visible problem, and 120ms sat inside the budget. I offset it by cutting k from 20 to 5, which recovered latency in the generation stage <em>and</em> reduced token cost, so the net latency change was small.<br><br><strong>The move that often dissolves the tradeoff:</strong> attack <em>perceived</em> latency instead. Streaming makes a 3-second response feel immediate because TTFT is what users experience. Showing intermediate progress transforms a 30-second agent task from broken-feeling to acceptable. That frequently buys more user-perceived improvement than any accuracy/latency compromise.<br><br><strong>And routing beats a uniform compromise:</strong> most traffic is easy, so a fast path for the common case plus an expensive path for the hard tail dominates picking one middle setting for everything.<br><br><strong>The thing I would not do:</strong> optimize latency without a quality eval running. That reliably ships something faster and worse.` },

{ q: 'How would you handle quality degrading over time?', tags: [], a: `<strong>First establish that it is real and locate it,</strong> since "it feels worse" is not actionable. Run your eval suite against current production config and compare to the baseline. If you do not have that suite, that is the first finding — you cannot detect drift you never measured.<br><br><strong>Then work through the causes, roughly by likelihood:</strong><br>• <em>The model changed underneath you.</em> A provider updating a floating endpoint alters behavior with no commit on your side. This is the most common cause and the most overlooked — check whether you pinned the version.<br>• <em>Data drift</em> — the query distribution shifted. Users found new use cases, or a marketing push brought a different population. Compare recent query embeddings against your eval set distribution.<br>• <em>Corpus staleness</em> — for RAG, documents changed, were deleted-but-not-removed from the index, or grew stale. Stale content produces confident wrong answers with no error.<br>• <em>Index growth</em> — more documents means more plausible-but-wrong near neighbors, so precision falls even at constant recall.<br>• <em>Accumulated changes</em> — a sequence of small prompt edits that each seemed fine.<br><br><strong>The process fix is the real answer:</strong> continuous evaluation on a schedule against production config, not just on commits; monitoring of leading indicators (schema-failure rate, guardrail triggers, regeneration rate); and mining production failures back into the eval set so it stays representative.<br><br><strong>Pin your model versions</strong> so upgrades are a decision, not an event.` },

{ q: 'How do you communicate AI limitations to non-technical stakeholders?', tags: [], a: `<strong>Lead with the operational consequence, not the mechanism.</strong> "It will be wrong about 1 in 20 times, so we need a review step for anything that touches money" lands. "It's an autoregressive model optimizing next-token likelihood" does not.<br><br><strong>Use analogies that carry the right intuition.</strong> The one I find most useful: it is like a very well-read new hire who has never seen your company's data, will confidently answer anything you ask, and does not reliably know what they do not know. That single framing explains hallucination, the need for RAG, and the need for review — without a word of technical vocabulary.<br><br><strong>Show, do not describe.</strong> Three real failure examples from your eval set do more for calibration than any explanation. Stakeholders anchor on demos, so if you only show successes, you have set an expectation you will not meet.<br><br><strong>Quantify with a commitment they can hold you to:</strong> "85% auto-accepted, 15% routed to review" is verifiable. "It works well" is not, and it will be interpreted optimistically.<br><br><strong>Be specific about which limitations are fixable and which are not.</strong> "Retrieval quality we can improve with engineering; the model occasionally being confidently wrong is a property we design around, not one we remove." That distinction prevents both false hope and unwarranted pessimism.<br><br><strong>Frame the human layer as design, not failure.</strong> Review and escalation are how you make a probabilistic system safe — presenting them as shortcomings invites pressure to remove them.` },

{ q: 'How would you build an AI feature with limited labeled data?', tags: [], a: `<strong>Reframe the constraint first:</strong> with foundation models you often need far less labeled data than the question assumes, because you are not training from scratch. The question becomes what you need labels <em>for</em>.<br><br><strong>The order I would work through:</strong><br>1. <strong>Zero/few-shot prompting.</strong> Frequently sufficient, and requires only a handful of examples. Always the baseline to beat.<br>2. <strong>Build the eval set first, even if small.</strong> 50–100 human-verified examples is enough to measure reliably, and this is the one place labels are non-negotiable — you cannot evaluate a synthetic-trained model on synthetic labels.<br>3. <strong>RAG</strong> if the gap is knowledge rather than behavior. This needs no labels at all, which is why it is often the right answer to "we have no data."<br>4. <strong>Synthetic data generation</strong> — use a strong model to generate training examples, ideally with <em>rejection sampling</em> against a verifier where the domain allows (math, code, structured extraction). That gives quality filtering without human labels.<br>5. <strong>Active learning</strong> — label the examples where the model is least confident or where an ensemble disagrees. This is far more label-efficient than random sampling and is the highest-value use of scarce annotator time.<br>6. <strong>Weak supervision</strong> — heuristics and rules generating noisy labels, denoised programmatically.<br><br><strong>The point to emphasize:</strong> spend your limited labeling budget on <em>evaluation</em>, not training. A good eval set with prompting beats a mediocre fine-tune with no way to measure it.` },

{ q: 'Describe your experience working with cross-functional teams.', tags: [], a: `<strong>Name the specific friction points AI projects create,</strong> since that is what the question is really probing.<br><br><strong>With product:</strong> the recurring issue is that AI features cannot be specified as deterministic behavior. What works is converting requirements into measurable targets early — "85% auto-accepted with the rest to review" rather than "it should read documents." That turns an unbounded expectation into a testable commitment, and it surfaces disagreements before engineering starts.<br><br><strong>With design:</strong> the interesting work is designing for uncertainty — what a low-confidence answer looks like, how errors are surfaced, how a user corrects the system. AI features fail in UX more often than in modeling, and designers who understand that the system is probabilistic produce far better flows.<br><br><strong>With legal and compliance:</strong> engage early rather than at launch. Data residency, retention, and automated-decision constraints are <em>architectural</em> — retrofitting audit logging or moving personal data out of training after the fact is expensive or impossible.<br><br><strong>With domain experts:</strong> they are the only people who can build a credible eval set in specialized domains, and non-expert labels are close to worthless in medical, legal, or financial work. Getting their time is usually the project's real bottleneck.<br><br><strong>The habit that helps most:</strong> share failure examples widely and early. Nothing aligns a cross-functional team on what the system actually does faster than everyone looking at the same twenty real outputs.` },

{ q: 'Where do you see AI engineering heading in 3-5 years?', tags: [], a: `<strong>Give calibrated predictions with reasoning, not a hype list.</strong> Interviewers are testing judgment about what matters, not forecasting accuracy.<br><br><strong>High confidence:</strong><br>• <em>Inference-time compute becomes a first-class scaling axis.</em> Reasoning models established that you can buy capability with tokens at inference, which turns "how much thinking should this query get" into a routine engineering decision — a cost/quality dial that did not exist before.<br>• <em>Cost per token keeps falling sharply.</em> That changes what is economically viable more than capability gains do; architectures that are absurd today become default.<br>• <em>Evaluation infrastructure matures</em> and becomes the real differentiator between teams. It already is, but the tooling is immature.<br>• <em>Small specialized models take a large share of production volume,</em> with frontier models reserved for the hard tail. The economics are too favorable to ignore.<br><br><strong>Medium confidence:</strong> agents become reliable enough for genuinely autonomous multi-step work in bounded domains — currently the reliability math (compounding per-step error) is the binding constraint, not intelligence. And standardization of tool and agent interfaces (MCP and successors) reduces integration cost substantially.<br><br><strong>What I think is overhyped:</strong> fully autonomous general-purpose agents, and the assumption that context windows growing solves retrieval — usable context lags advertised context, and targeted retrieval keeps winning on cost and quality.<br><br><strong>What does not change:</strong> data quality, evaluation discipline, and the latency/cost/reliability tradeoff. Those have been the actual work for years and will remain it.` },

{ q: 'Why are you interested in this AI engineering role?', tags: [], a: `<strong>This question tests whether you have specific reasons or generic enthusiasm.</strong> A strong answer connects three things: what the company is actually building, what you have done, and what you want to work on next.<br><br><strong>The structure that works:</strong><br>• <em>Something specific about their problem.</em> Reference the actual technical challenge — the scale, the domain constraint, the product surface. "You're doing RAG over regulated financial documents, which means parsing quality and audit trails are the hard parts, not the model" demonstrates you have thought about their problem rather than read their careers page.<br>• <em>The connection to your experience.</em> Not a résumé recap — one concrete thing you have solved that maps to what they face.<br>• <em>What you want to learn.</em> Naming a genuine gap reads as self-aware rather than as weakness, and it signals you are choosing this role over others for a reason.<br><br><strong>What weakens the answer:</strong> "AI is exciting and moving fast" — true of every AI role and therefore says nothing. Praising the company's mission without technical specifics. And overclaiming interest in something you cannot discuss in depth, because the follow-up question will expose it.<br><br><strong>The credibility signal:</strong> being able to name a tradeoff <em>in their</em> product that you find interesting. That demonstrates you have engaged with the actual engineering rather than the marketing — and it usually turns the answer into a technical conversation, which is where you want to be.` },

{ q: 'Your PM wants to ship with a 15% hallucination rate on edge cases. How do you communicate the risk?', tags: ['CRITICAL'], a: `<strong>Do not lead with the number — lead with the consequence.</strong> "15%" is abstract; "roughly 1 in 7 of these queries gets a confidently wrong answer, and the user has no way to tell" is not. Then quantify the exposure: how many users hit edge cases per week, and what happens to each one.<br><br><strong>Make the harm concrete and specific.</strong> Show three real failure examples from the eval set. A PM reading an actual fabricated answer that a customer would receive makes a better decision than one reading a percentage.<br><br><strong>Then present options rather than a veto,</strong> because "we can't ship" is rarely the useful contribution:<br>• <em>Detect and abstain</em> — if the edge cases are identifiable (low retrieval confidence, a query-type classifier), refuse or escalate on those rather than answering. This is usually the highest-leverage fix and often converts 15% wrong into 15% "I don't know," which is a completely different risk profile.<br>• <em>Scope down</em> — ship for the query classes that work, exclude the rest.<br>• <em>Add a human gate</em> for the affected class.<br>• <em>Ship with visible uncertainty</em> and a correction path.<br><br><strong>State the asymmetry explicitly:</strong> a wrong answer costs more than no answer in most domains, because it is acted upon. And a confident wrong answer damages trust in the whole feature, not just that response.<br><br><strong>Then respect the decision.</strong> If the PM accepts the risk with the information in hand, document it, add monitoring and a kill switch, and ship. Your job was to make the tradeoff visible, not to make the call.` },

{ q: 'An executive asks why your AI cannot be 100% accurate. How do you explain it?', tags: [], a: `<strong>Give the mechanism in one sentence, without jargon:</strong> the system predicts the most plausible response based on patterns in what it has read — it does not look up facts and verify them, so a fabrication and a fact are produced the same way. That single framing explains why it is confidently wrong sometimes, and it takes fifteen seconds.<br><br><strong>Then reframe the goal, which is the more important move.</strong> 100% accuracy is not the standard we hold anything to — human agents make errors, existing processes have error rates, and those are managed with review and correction rather than eliminated. The right question is not "why isn't it perfect" but "is it better than what we do today, and is the error rate manageable at an acceptable cost?"<br><br><strong>Make the comparison concrete</strong> where you can: if manual review has a 4% error rate and takes 20 minutes, and the system has a 6% error rate at 3 seconds with a review step catching most of them, that is a favorable trade with numbers attached.<br><br><strong>Distinguish fixable from inherent.</strong> "Retrieval quality we improve with engineering. The model occasionally producing a confident wrong answer is a property of the technology — we design around it with verification and human review." That prevents both false hope and the assumption that more budget makes it perfect.<br><br><strong>Close on the design implication:</strong> this is why the architecture includes citations, confidence thresholds, and a review path. Those are not compensating for a defective system — they are how you build a reliable process around a probabilistic component.` },

{ q: 'Choose between a complex agentic system scoring 15% better, or a simpler RAG pipeline. How do you decide?', tags: ['CRITICAL'], a: `<strong>Interrogate the 15% first, because benchmark deltas frequently do not survive contact with production.</strong> Questions I would ask: measured on what — a public benchmark or <em>our</em> traffic distribution? Over how many runs, and with what variance? Agents are non-deterministic, so a single-run 15% could be noise. Does the gain concentrate in a query class that is rare in our actual traffic? And what are the cost and p95 latency at that quality?<br><br><strong>Then price the complexity honestly,</strong> since that is what the question is really testing:<br>• <em>Debuggability</em> — a RAG failure is traceable to retrieval or generation in minutes; an agent failure means reading trajectories with non-deterministic control flow.<br>• <em>Cost and latency</em> — agents make multiple model calls, so typically several times the spend and latency.<br>• <em>Reliability math</em> — multi-step systems compound per-step error, and they fail in ways that are harder to bound.<br>• <em>Operational burden</em> — budgets, loop detection, step caps, and a much larger surface for prompt injection if tools are involved.<br><br><strong>My default is the simpler system,</strong> and the burden of proof sits with the complex one. A 15% benchmark gain that costs 4x and takes a week to debug per incident is usually a bad trade.<br><br><strong>What would change my mind:</strong> if the gain is on queries that genuinely require multi-step retrieval — where single-shot retrieval <em>structurally cannot</em> answer — then it is not an optimization, it is a capability. In that case I would route: cheap RAG path for the majority, agent for the classified multi-hop minority. That captures the gain where it is real without paying for it everywhere.` },
]};
